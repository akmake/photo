# הנחיות לכל בינה מלאכותית שעובדת על הפרויקט הזה

## 0. קודם כול: זו התוכנה, לא האתר

המוצר בנוי משני חלקים נפרדים, בשתי תיקיות נפרדות:

| | **התוכנה — התיקייה הזו** | **האתר** |
|---|---|---|
| נתיב | `C:\Users\yosef dahan\Documents\GitHub\photo` | `C:\Users\yosef dahan\Documents\GitHub\ManagPhoto` |
| GitHub | `akmake/photo` | `akmake/ManagPhoto` |
| איפה רץ | אצל הצלם על המחשב. נארז לקובץ EXE ומופץ. **לא באינטרנט** | באינטרנט. שם כל התנועה |
| מי משתמש | הצלם בלבד | צלמים שקונים מנוי, והלקוחות של הצלמים |

### מה שייך לתוכנה (לכאן)
- כל העבודה על התמונות: עריכה, ריטוש, צבע, אלבום, ניהול העבודה של הצלם.
- מנוע העריכה, ה-RAW, הייצוא.
- התמונות המקוריות. הן נשארות על הדיסק של הצלם ולעולם לא עולות לאתר.

### מה שייך לאתר (לא לכאן)
- דף הנחיתה, המחירון, הקנייה והמנוי.
- פתיחת משתמש, כניסה, אזור הצלם.
- הנפקת הרישיון שהתוכנה בודקת.
- הגלריה של הלקוח: הלקוח של הצלם נכנס, רואה את התמונות שלו ובוחר מהן.

### מה עובר ביניהם
- **מהתוכנה לאתר:** תמונות תצוגה לגלריה (לא המקור).
- **מהאתר לתוכנה:** הרישיון, ומה שהלקוח בחר או העיר בגלריה.
- **הגלריה ללקוח רצה באתר**, לא כאן. הסטודיו קורא `/api/gallery/*` במנוע כמו
  תמיד; `engine/gallery_remote.py` מעביר לאתר, מזדהה ב-lease של הרישיון, וגוזר
  ומעלה תצוגות (1600px + 400px). המקור לא עולה. הקישור ללקוח: `<אתר>/g/<slug>`.
- **עדכוני תוכנה:** התוכנה שואלת `<כתובת הרישוי>/updates/latest.yml` ומורידה משם
  את קובץ ההתקנה. האתר רק מגיש קבצים מתיקייה קבועה בשרת; אין שם לוגיקה.
  התוכנה: `electron/updater.cjs`. ההעלאה: `npm run release` (בונה ומעלה,
  `scripts/release-upload.cjs`). כללים: `latest.yml` בלי מטמון; גרסאות ישנות לא
  נמחקות מהשרת (ההורדה החלקית צריכה את ה-`.blockmap` הקודם); חזרה לאחור = לפרסם
  בנייה ישנה במספר גרסה חדש, כי התוכנה לא יורדת גרסה.
- **כתובת האתר לא קבועה בתוכנה.** קובץ ההתקנה נושא כתובת ראשונה בלבד; מאז היא
  שמורה אצל הצלם (`server.json` בתיקיית הנתונים) ומשמשת גם לרישיון וגם לעדכונים.
  מעבר דומיין: `npm run server:move -- https://חדש` כותב `updates/server.json`
  בשרת הישן, והתוכנות עוברות לבד בבדיקת העדכון הבאה — רק אחרי שהכתובת החדשה
  מחזיקה את אותו מפתח רישוי. גיבוי: שדה "כתובת השרת" בהגדרות. הדומיין הישן חייב
  להישאר חי כמה חודשים אחרי מעבר. קוד: `electron/serverOrigin.cjs`,
  `engine/license_state.py` (`server_origin`). **לעולם לא לקרוא את הכתובת
  ישירות מ-`license-config.json` או מ-`TEZA_LICENSE_ORIGIN`.**

אין כאן שרת ציבורי, אין כתובת באינטרנט ואין תנועה של משתמשים. הצעה שמניחה
שהתוכנה "יושבת באינטרנט" שגויה מהיסוד. עבודה על קהל, קנייה, חשבון, רישיון או
הגלריה של הלקוח נעשית בתיקיית האתר.

שינוי שחוצה את הגבול (למשל שינוי במבנה הרישיון או במה שהגלריה מחזירה) מחייב
שינוי תואם בשני הצדדים. לבדוק את הצד השני לפני שמשנים, ולא להניח.

### עותק האתר שהיה כאן נמחק
עד 23.09.2026 הייתה כאן תיקייה `server\` עם עותק של האתר. היא נמחקה (הייתה זהה
לאתר). אם מסמך ישן מפנה ל-`server\` — הכוונה לתיקיית האתר.

---

## 1. איך עונים כאן

שלושה כללים. הם נכתבו אחרי שחזר ונשנה הדפוס: טענה בקול בטוח → הצלם שואל
עליה → "רגע, אני מאמת". כלומר הדחיפה שלו הייתה בקרת האיכות, והוא שילם עליה
בטוקנים.

**א. הבנה — מקסימלית. תמיד.**
להחזיק את המערכת כולה ולחשוב ברמת המוצר, לא ברמת המשפט שנכתב. הצלם בונה
מוצר שאנשים יאהבו, לא MVP — לשונו: *"התוכנה נמדדת בדברים הקטנים; אנשים רוצים
96%, לא 95%."* לפני שבונים כלי, לשאול *"האם זה עושה את הרעיון מקצועית?"* אם
לא — לעצור ולבנות נכון, לא להתקדם.

**ב. פעולה — בדיוק מה שהתבקש. לא פחות, ובעיקר לא יותר.**
כלל א' חל על **חשיבה**, לא על **קוד**. תלונה בת שורה אחת אינה מנדט לשינוי
ארכיטקטוני. ארכיטקטורה — מציעים ומקבלים "כן" לפני שנוגעים. כשבקשה עמומה
(במיוחד "אני לא רוצה את זה") — **שאלת הבהרה אחת מדויקת לפני שנוגעים בקוד**,
אף פעם לא ניחוש כיוון.

> ב-06.08.2026 הוא הצביע על כרטיס אחד בספריית האלבומים ואמר "אני לא רוצה את
> זה". במקום לשאול "העיצוב של הכרטיס, או כל הרשימה?" — נמחק כל מסך הספרייה
> והומצאה לוגיקת אלבום-אוטומטי-לפרויקט. הוא הזהיר מזה בהודעה הראשונה של אותו
> סשן. הוא חווה סקופ לא מבוקש כדריסת הסמכות שלו על המוצר שלו.

**ג. אימות — לפני הטענה, לא אחרי הדחיפה.**
- טענה שמשנה מה הוא יבנה או יחליט — **בודקים לפני שאומרים אותה.**
- לא בדקת? אומרים "לא בדקתי" **באותה שורה**, לא בקול של עובדה.
- שאל על משהו שכן נבדק? **עונים מהראיה שכבר קיימת.** שאלה אינה הודעת שגיאה.
- עבודה נסתרת עולה לו כסף והוא לא רואה אותה. לקרוא **ממוקד** — את הקובץ
  הנכון, לא את הריפו.

**ד. תיעוד עבודה — טיים־ליין מצטבר, לא סיכום שנכתב בדיעבד.**
בכל מסלול אבחון/שיפור מתמשך מתעדים בזמן העבודה: קלטים, מדידת בסיס, השערה,
ניסוי שנדחה, החלטה, שינוי, ומדידת אחרי. לא מוחקים כשל שנלמד ממנו ולא משכתבים
מספר ישן כדי שיתאים לתוצאה החדשה. מסלול ColorMatch מתועד ב־
`docs/COLORMATCH-TIMELINE.md`; כל פעולה עתידית בכלי הזה מתווספת לשם לפי הסדר.

**ה. שיטת העבודה — חובה בכל בעיה, באג או כלי:** לפני שנוגעים בקוד, לפעול לפי
הכלי `work-method` (`.claude/skills/work-method/SKILL.md`).

**החריג:** באיטרציה מהירה על UI/CSS — רק לגעת בקוד. בלי `tsc`, בלי דפדפן,
בלי צילומי מסך. שרת הפיתוח שלו רץ וה-HMR מראה לו מיד, אז אימות שלנו הוא רעש.
**החריג הזה חל על CSS ופריסה בלבד** — לא על המנוע, לא על מודל הנתונים ולא על
ארכיטקטורה. שם כלל ג' חל במלואו.

---

## 2. מפת ניתוב — לא לקרוא הכול, לקרוא את הנכון

`docs/ARCHITECTURE.md` הוא **מקור-האמת** של הפרויקט: חוזה הכלי האחיד, מודל
שלוש השכבות, `MaskProvider`, כללי מוצר, ומדידות לפני/אחרי לכל שלב בריטוש.
**כל שאלה על מנוע, כלי, מסכות או מודל נתונים מתחילה שם.**

### מסמכים

| מתי | קובץ |
|---|---|
| מנוע, כלים, מודל נתונים, כללי מוצר | `docs/ARCHITECTURE.md` |
| רישוי, מנוי, חשבון, נעילה, שרת רישיונות | `docs/LICENSING.md` |
| באג ידוע / מדידה קיימת | `docs/BUGS.md` |
| מה מצבו של כלי מסוים | `docs/TOOLS-STATUS.md` |
| UX, מסכים, שלד המוצר | `docs/PRODUCT-UX.md` · `UX-SKELETON.md` |
| Lab מול שולחן העבודה | `docs/LAB-VS-WORKBENCH.md` |
| ביצועים ואיטיות | `docs/SLOW.md` · `opo/machine-scaling.md` |
| אלבום חכם | `alb.md` · `docs/ALBUM-MACHINE.md` · `docs/RESEARCH-album-ai.md` · `docs/new/album-visual-language.md` |
| גלריית לקוח, בחירה והערות | `docs/CLIENT-GALLERY.md` — **הגלריה עצמה באתר מאז 24.09**; כאן רק `engine/gallery_remote.py` + `gallery_derive.py` |

### קוד

| כשמדובר ב… | ללכת ל… |
|---|---|
| **כלי עריכה כלשהו** | `src/toolRegistry.ts` = הקטלוג (id, פרמטרים, קטגוריה) · `engine/<שם>.py` = המימוש |
| ניקוי עור, כתמים, פיגמנט | `engine/cleanup.py` · `skinmodel.py` · `pigment.py` · `healing.py` |
| "איפה נמצא X בתמונה" | `engine/masks.py` — **מקום אחד**. כלי AI חדש = לבחור `kind`, לא לחבר מודל |
| ה-API של המנוע | `engine/server.py` (`127.0.0.1:8756`) |
| הפרויקט על הדיסק, סשנים, שכבות מתכון | `engine/workspace.py` · `src/studio/store.ts` · `src/types.ts` |
| מונגו | `engine/db.py` |
| התאמת צבעים | `engine/local_color.py` · `color_harmonization.py` · `src/studio/screens/ColorMatch.tsx` |
| אלבום חכם | `src/album/*` · `engine/album_*.py` · `cull.py` · `embed.py` · `identity.py` |
| מסכי הסטודיו | `src/studio/screens/*` · `nav.ts` · `Shell.tsx` |
| שולחן העבודה של הסט | `src/studio/screens/SetWorkbench.tsx` |
| Lab | `src/lab/*` |
| עיצוב | `src/design/tokens.css` ואז ה-CSS של המסך הספציפי |
| RAW / ייצוא | `engine/raw.py` · `render.py` · `lut.py` |
| עדכוני תוכנה | `electron/updater.cjs` · `src/update/UpdateBar.tsx` · `scripts/release-upload.cjs` |

### מלכודות

- **`engine/_*.py`** (קידומת קו תחתון) — סקריפטי אבחון חד-פעמיים, **לא מוצר**.
  יש עשרות. לא לתקן אותם ולא ללמוד מהם על התנהגות נוכחית.
- **`test-results/`** — פלט של ריצות עבר. תמונות, לא קוד מקור.
- **`src/album/layoutEngine.ts`** — מחולל contact-sheet. **לעולם לא לחווט
  אותו לאלבום האוטומטי.**
- **`_archive_chabad/`**, **`teza-new/`**, **`experiments/`** — לא המוצר.
- מסמך בן חודש יכול להיות לא מעודכן. אם הוא סותר את הקוד — **הקוד הוא
  האמת, והסתירה מדווחת למשתמש.**

---

## 3. ענף אחד בלבד: `main`. זה כלל מוחלט.

**אסור ליצור ענף. אסור לעבור ענף. הכול נשמר על `main`, תמיד.**

```
git checkout -b <name>     ❌ אסור
git switch -c <name>       ❌ אסור
git checkout <other>       ❌ אסור
git worktree add           ❌ אסור
```

אם נראה לך שמשימה "מתאימה לענף נפרד" — היא לא. תעבוד על `main`, תקמט לעיתים
קרובות, ותתאר בהודעת הקומיט מה עשית.

אם קיים ענף כלשהו שאינו `main` — תדווח עליו למשתמש מיד. אל תמזג ואל תמחק בלי
אישור מפורש.

### למה הכלל הזה קיים

ב-04.08.2026 בשעה 23:22:08 בוצע מעבר לענף `fold-protection`. **מעבר ענף מחליף
פיזית את הקבצים על הדיסק** — אז מסך הפרויקטים, החנות ו-`Today` נדרסו בגרסאות
מלפני חמישה ימים, ו-`SetWorkbench.tsx`, `Batches.tsx`, `Clients.tsx` נמחקו
מהדיסק לגמרי.

שלוש דקות אחר כך התחילה סשן של Claude, ראתה את הקבצים הישנים, הניחה שזה המצב
הנוכחי — ובנתה מחדש, גרוע יותר, מערכת שכבר הייתה בנויה. הצלם ראה בבוקר מוצר
אחר לגמרי משהשאיר בלילה, והיה משוכנע שאיבד את עבודתו.

שום דבר לא אבד בסוף, אבל זה עלה לילה שלם. הכלל הזה קיים כדי שזה לא יקרה שוב.

### מה שכן חובה לעשות במקום

**לקמט. הרבה.** באותו אירוע היו חמישה ימי עבודה לא מקומטים — כולל 64 שורות
מנוע שלא היו שמורות בשום מקום. תקמט כשיחידת עבודה עומדת בפני עצמה, ולא רק
כשמישהו מבקש.

לפני שאתה נוגע בקוד, ודא על מה אתה יושב:

```
git status -sb        על איזה ענף, ומה לא שמור
git log --oneline -5  מה נעשה לאחרונה
```

אם `git status` מראה ענף שאינו `main`, או ערימת שינויים לא שמורים — **עצור
ותדווח למשתמש לפני שאתה משנה משהו.**

---

## 4. מה שמור איפה

| מה | איפה | הערה |
|---|---|---|
| התמונות | הדיסק, במקום שהצלם שם אותן | לעולם לא מועתקות למסד |
| ידע על העבודה | MongoDB מקומי, מסד `teza` | דרך המנוע: `engine/db.py` |
| מצב נוכחי של הפרויקטים | עדיין `localStorage` | המונגו מחובר אך לא מחווט ל-UI |

הדפדפן לא מדבר עם מונגו. המנוע (`127.0.0.1:8756`) הוא שרת הנתונים, והוא גם
היחיד עם גישה לדיסק.

---

## 5. אסור להמציא נתונים

אין `SEED`, אין קבצי דמה, אין מספרים לדוגמה במסך שנראה אמיתי. מסך ריק שאומר
"אין עדיין פרויקטים" הוא אמת; מסך מלא בלקוחות מומצאים הוא שקר שהצלם מתכנן
לפיו.

**ריק ≠ לא נקרא.** אם קריאה מהמסד נכשלה, המסך חייב לומר "לא ניתן לקרוא", לא
"אין נתונים" — ואסור לכתוב על נתונים שלא הצלחת לקרוא.

בעיה אמיתית שקרתה: `load()` החזיר 12 פרויקטי דמה כשהרשימה הייתה ריקה, והשמירה
הבאה דרסה בהם פרויקט אמיתי.

---

## 6. Windows, עברית וקידוד

הפרויקט רץ על Windows עברי. **אל תניח UTF-8 בשום גבול בין תהליכים.**

בעיה אמיתית שקרתה: חלון בחירת התיקייה החזיר `C:\...\?????` במקום עברית, כי
PowerShell כותב לפי קוד־דף הקונסולה ופייתון קורא לפי קוד־דף המערכת. הייבוא נשבר
על כל תיקייה בעברית. הפתרון: להעביר base64 בין התהליכים (`engine/server.py`,
`_pick_folder`).

כשקוראים ל-subprocess שמחזיר נתיב — תמיד קידוד מפורש, לא ברירת מחדל.

**כישלון אינו ביטול.** אותו באג הסתתר כי פלט ריק דווח כ"המשתמש ביטל", והצד
הקורא יוצא בשקט על ביטול. כפתור שלא עושה כלום ולא אומר כלום הוא הבאג הכי יקר
לאיתור — תמיד תבדיל בין השניים.
