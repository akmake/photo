# שורשי הבעיות בשני כלי הניקוי, ותוכנית התיקון

נלווה ל-[AUDIT-CLEANUP-11.md](AUDIT-CLEANUP-11.md), שמתעד את הסימפטומים תמונה-תמונה.
כאן: **למה** זה קורה, בקוד, ומה מתקנים — לפי סדר.

---

## הממצא שמנהל את כל השאר: **הסליידר אינו הבעיה**

סריקה מלאה של חוגת `spots` על כל 23 התמונות (40 פנים):

| `spots` | מועמדים | ריפויים | מהם על אנטומיה | **% שווא** | על השפה | ‏`spot` | ‏`fluid` | ‏`debris` |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 0 | 116 | 54 | 27 | **50%** | 16 | 36 | 11 | 7 |
| 10 | 136 | 63 | 35 | **56%** | 21 | 45 | 11 | 7 |
| **25** (היום) | 179 | 87 | 49 | **56%** | 31 | 69 | 11 | 7 |
| 40 | 251 | 130 | 70 | **54%** | 41 | 110 | 13 | 7 |
| 100 | 845 | 583 | 214 | 37% | 68 | 566 | 12 | 5 |

**שיעור השווא שטוח לאורך כל החוגה.** להוריד את `spots` לא קונה דיוק — הוא רק קונה
פחות עבודה, ומוותר על ממצאים אמיתיים באותו יחס. **מכאן: אין ערך ברירת מחדל שפותר את
הבעיה.** מי שמכוון את הסליידר מכוון את הכמות, לא את האיכות. צריך לשנות את השערים.

(המספר 37% ב-100 אינו שיפור: שם נוספו 500 ריפויים על עור פתוח, שמדללים את היחס.)

---

## שורש 1 — **הרף מנוסח בסיגמות, אף פעם לא בעוצמה**

**הקוד.** ‏`cleanup._novelty_bar` ‏(`cleanup.py:206`):

```python
return 4.5 - strength * 3.0        # spots=25  ->  lo = 3.75 sigma
```

ואז (`cleanup.py:913`):

```python
lo = _novelty_bar(strength); hi = lo + 1.6
c = smoothstep((novelty - lo) / (hi - lo)) * region
```

‏`novelty` הוא מרחק מהלנוביס מהמודל המקומי של העור. **בשום מקום בשרשרת לא נשאל כמה
גדולה הסטייה בפועל** — לא ב-L*, לא ב-a*, לא ברמות.

**ההשלכה, וזו הנקודה שמסבירה את כל הביקורת:** ככל שהעור **נקי יותר**, הסיגמה של המודל
**קטנה יותר**, ולכן רמפה אנטומית רגילה — רכס העפעף התחתון, הפילטרום, קמט כנף האף —
חוצה 3.75 סיגמה בקלות בזמן שהיא בלתי-נראית לעין. הכלי לא "טועה יותר על פנים בעייתיות";
הוא טועה יותר **על פנים נקיות**.

**וזה בדיוק מה שנמדד:**

* ‏`321A5117` — נערה, 539px, עור ללא רבב. שני הריפויים היחידים: רכס העפעף והפילטרום.
* ‏`321A4934` — שני ילדים, עור נקי. ‏**6 מתוך 6** הריפויים בתוך 0.10 מגבה או שפה.
* ‏`321A5127` — ‏`noveltyPeak` ‏4.39 ו-4.44, ‏`confidencePeak` ‏1.000, על עור ריק לגמרי.
* ומנגד `321A1791`, ילדה עם גירוי עור אמיתי: 14 ריפויים, כולם במקום.

**התיקון שהצעתי תחילה — רצפת עוצמה מוחלטת — נבדק ונדחה.** ‏`_gt_contrast.py` מדד
ΔL*/Δa*/Δb* של כל 88 הריפויים מול טבעת העור שסביבם. התוצאה **סותרת** את ההשערה:

```
ממצאי השווא שאומתו בזום:  5117#1  dL=+7.0   5117#2  dL=+9.0   (עוצמה גבוהה!)
השומה האמיתית ב-5127#3:                              mag=1.41  (הנמוכה בסט)
רצפה של 4 רמות: משאירה 60 מ-88, ושיעור-האנטומיה נשאר 57%
```

כלומר האנטומיה **חזקה** ולא חלשה, ורצפת עוצמה הייתה מוחקת כתמים קטנים אמיתיים ומשאירה
את רכס העפעף. ההשערה בצורתה הזאת מתה. *(הסתייגות מדידה: החציון על פני המסכה מדלל
ליבה כהה קטנה — השומה יצאה `dL=0.00` למרות שהיא כהה בבירור לעין. מדידה עתידית צריכה
אחוזון, לא חציון.)*

**מה שכן עלה מהמדידה, וזה ממצא חדש:** ממצאי השווא על אנטומיה הם ברובם **הבהקים** —
בהירים מהסביבה ולא אדומים ממנה — בעוד שכתם אמיתי הוא אירוע **פיגמנט**: כהה ו/או אדום.

```
כלל נבדק: לפסול מועמד מסוג spot שהוא בהיר מהסביבה (dL>=+3) ולא אדום ממנה (da<+1.5)
  נפסלים 17 מתוך 69 ריפויי spot, מהם 10 על אנטומיה
  הרשימה נשלטת על ידי נחיר / שפה / מסגרת-עין / גבה — כולל שני הממצאים של 5117 שאומתו
  מעבר ה-fluid (רוק) לא נפגע כלל: אף אחד מ-19 ה-fluid/debris לא נפסל
```

זה תיקון אמיתי אבל **חלקי**: הוא מוריד 10 ממצאי שווא, ולא מזיז את היחס הכולל (‏57% ⇦ 56%),
כי הוא מפיל גם 7 ממצאים על עור פתוח. הוא נכנס לתוכנית — אבל לא כפריט הראשון.

---

## שורש 2 — **ההגנה היא גאומטריה בינארית, ולכן כל *מעבר* אל תו פנים חשוף**

**הקוד.** ‏`masks.anatomy_parts` ‏(`masks.py:314`) בונה קמורים של טבעות ציוני-הדרך —
עין, גבה, שפתיים, אף — פלוס דיסקית `EYE_CORNER_CAP` שנוספה כתיקון ל-BUG-004. בפנים
= מוגן, בחוץ = הפקר.

**ההשלכה.** כל הכשלים שמצאתי יושבים **ברצועה שמיד מחוץ לקמור**: רכס העפעף התחתון
(מתחת לקמור העין), הפילטרום (מעל קמור השפתיים), קמט כנף האף, פינת הפה. ‏BUG-004 היה
בדיוק אותו כשל בפינת העין, ותוקן **נקודתית**. מי שממשיך בשיטה הזאת יגלה עוד רצועה
בכל פעם.

**התיקון.** להחליף את הבינארי ב**רף שגדל עם הקרבה לתו פנים**: במקום "בפנים/בחוץ",
‏`lo` עולה כפונקציה של המרחק לציון-הדרך הקרוב. ליד תו פנים דורשים **יותר** ראיות —
לא אפס ראיות ולא חסינות מוחלטת, כך שנגע אמיתי על העפעף עדיין ניתן לטיפול.
זה שינוי אחד שמכסה את כל הדיסקיות הנקודתיות שאחרת ייכתבו אחת-אחת.

**איך מודדים:** ‏`_gt_bench.py` כבר מחשב `distFaceD` לכל מועמד. סורקים את שיפוע הרמפה
וקוראים את הספירות לפי `nearest` — בדיוק כמו סריקת הרדיוס שקבעה את `EYE_CORNER_CAP=0.08`.

---

## שורש 3 — **שיער פנים אינו "אנטומיה" בשום מקום**

**הקוד.** ‏`cleanup.py:909` מחסיר `hair` מ-`support` — כלומר משיער **נדגם** המודל,
וזה נכון. אבל `region`, האזור שמותר לרפא בו, לא מוגן ממנו.

**ההשלכה.** ממצאי השווא הגדולים ביותר בסט הם על **גבול הזקן והשפם** של הגברים:
‏`321A5078` (צורה משוננת על השפם), ‏`321A5177` (‏`cMax=88.3`), ‏`321A5015`.

**התיקון.** להחסיר את מסכת השיער, מורחבת, גם מ-`region`. אותו טיפול שמקבלים תווי הפנים.

---

## שורש 4 — **שער הגודל שותק**

**הקוד.** ‏`cleanup.py:862` ו-`:1378`:

```python
face_d = float(np.sqrt(skin.sum()))
if face_d < MIN_FACE_PX:      # 180
    return None
```

‏`faceTooSmall` אמנם מגיע ל-`meta`, אבל שום דבר בממשק לא מציג אותו.

**ההשלכה.** ‏11 מתוך 40 פנים, ו-4 תמונות שלמות, יוצאות זהות למקור. המשתמש רואה כלי
דלוק ולא רואה שינוי, ואין לו שום דרך לדעת למה.

**התיקון, שני חלקים:**
1. **מיידי:** להחזיר את הספירה למשתמש — "‏4 פנים בפריים קטנות מדי לניקוי". זה תיקון
   ממשק, לא אלגוריתמי, והוא הופך כשל שקט לכשל מוצהר.
2. **אחריו:** להכריע אם 180 הוא הרף הנכון. הבדיקה: להגדיל פנים קטנות אל מעל הרף,
   להריץ, ולהשוות מול הפלט המקורי. אם התוצאה מכובדת — הרף יורד או שהמנוע מגדיל בעצמו.

---

## שורש 5 — **החוזה מוצהר פעמיים, ואופרטור שלישי לא מוצהר באף אחד מהם**

**הקוד.** ‏`cleanup.py:1453`:

```python
gloss = common.clamp01(params.get("gloss", params.get("strength", 60)))
```

ה-UI שולח `redness` ו-`spots` בלבד → **הפחתת הברק רצה תמיד ב-60**, בלי סליידר, ובלי
הצהרה לא ב-`src/toolRegistry.ts` ולא ב-`engine/server.py`.

**התיקון.** רישום אחד, בבעלות המנוע, שה-UI קורא — הפתרון ש-`ARCHITECTURE.md` כבר מצביע
עליו. עד אז, לכל הפחות: לחשוף את `gloss` כסליידר בשני המקומות.

---

## שורש 6 — **לחוגה אין מצב כבוי**

ב-`spots=0` הכלי עדיין מבצע **54 ריפויים**. השורש: ‏`_novelty_bar(0) = 4.5` — רף סופי,
לא אינסופי, ואין בשום מקום בדיקה של `spots == 0`.

**התיקון.** ‏0 פירושו כבוי, במפורש. משתמש שמאפס סליידר מצפה שהאופרטור ייעצר.

---

## שורש 7 — **מחיקת שומות היא מדיניות, לא באג**

‏`321A5127#3`: שומה כהה, ‏`spot/heal`, נמחקת לגמרי. עקבי עם ההחלטה מ-2026-07-30.
**התיקון אינו בקוד הזיהוי** אלא בהצגה: בחירה גלויה בפריסט ("שמור שומות" / "נקה הכל"),
כך שמחיקת תו זיהוי תהיה תמיד תוצאה של בחירה.

---

## שורש 8 — **הפגם הנפוץ ביותר בסט אינו מטופל בכלל**

שערה בודדת שחוצה את הלחי או הסנטר מופיעה כמעט בכל פריים של הבנות. הכלי מזהה אותה
ופוסל כ-`line` — **בכוונה ובצדק**, כי מחיקת קווים אוטומטית מסוכנת. אבל אז אין כלום.
**התיקון: מכחול ריפוי ידני**, שכבר רשום כפריט פתוח ב-`TOOLS-STATUS.md`. זה גם ה-override
היחיד לשורשים 1, 2 ו-7 כשהמשתמש חולק על המכונה.

---

## סדר העבודה

| # | מה | סוג | למה קודם |
|---|---|---|---|
| 1 | ‏**רף שגדל עם הקרבה לתו פנים** (שורש 2) | אלגוריתמי | מחזיק את מסת ממצאי השווא — 49 מתוך 87 — ומכסה שפה, נחיר, גבה ועפעף במכה אחת במקום ארבע דיסקיות נקודתיות. עלה למקום הראשון אחרי שהמדידה הפילה את המועמד הקודם. |
| 2 | ‏**"כתם אינו הבהק"** (שורש 1, בנוסחה שכן שרדה מדידה) | אלגוריתמי | מוריד 10 ממצאי שווא על אנטומיה בלי לגעת ברוק. חלקי, אבל מדוד ובטוח. |
| 3 | ‏**החסרת שיער מ-`region`** (שורש 3) | אלגוריתמי | קטן, מקומי, מתקן את הממצאים הגדולים ביותר על גברים. |
| 4 | ‏**שער גודל מוצהר** (שורש 4א) | ממשק | הופך 28% כשל שקט לכשל גלוי. זול. |
| 5 | ‏**‏`gloss` ורישום אחד** (שורש 5) | חוזה | מונע את הכשל הבא מאותה משפחה. |
| 6 | ‏**‏0 = כבוי** (שורש 6) | התנהגות | ציפייה בסיסית של משתמש. |
| 7 | ‏**מדיניות שומות** (שורש 7) | מוצר | החלטה, לא קוד. |
| 8 | ‏**מכחול ידני** (שורש 8) | תכונה | הגדולה מכולן, וגם ה-override לכל השאר. |
| 9 | ‏**שער הגודל עצמו** (שורש 4ב) | אלגוריתמי | דורש את הניסוי של ההגדלה. |

**הכלל לאורך כל הדרך:** אחרי כל תיקון מריצים מחדש את אותה ספסל-בדיקה על אותן 23
התמונות (`_gt_bench.py` → `bench.json`) ומשווים שלושה מספרים: ריפויים, ריפויים על
אנטומיה, והאם ארבעת הממצאים האמיתיים שרדו — הפצעון של האישה ב-`5015`, הגירוי ב-`1791`,
הרוק ב-`1784`/`1819`, והשומות של האישה ב-`5078` שחייבות **להישאר**. שינוי שמשפר את
היחס ומאבד אחד מהם — נדחה.

---

## מה כבר נבנה ונמדד — הפריור המבני (2026-08-04)

תיקון 1 מהטבלה מומש: ‏`cleanup._structural_lift`. במקום "בתוך הקמור אסור, מחוץ לו
מותר", הרף הוא **שדה רציף** — ‏`lo + lift(x)` — עם שלושה איברים, וכל אחד מהם *נגזר
מכישלון נמדד*, לא מהעדפה:

| איבר | מה הוא אומר | מאיפה הוא בא |
|---|---|---|
| ‏`STRUCT_LIFT · near` | ככל שאתה קרוב למבנה מוגן, נדרשות יותר ראיות | ‏4 הדילטציות הידניות שכולן היו אותו טלאי |
| ‏`SPECULAR_LIFT · … · near` | בהיר וחסר-צבע **ליד מבנה** = תאורה | ‏5117: ‏`z_bright` ‏+5.0/+3.9, ‏`z_red` ‏−2.4/−1.6 |
| ‏`− PIGMENT_CREDIT · … · (1−near)` | אודם על עור פתוח = פיגמנט, מוריד את הרף | ‏1791: הממצאים האמיתיים ב-`z_red` ‏+1.6…+2.7 |

**שני הצימודים הם הלב, ושניהם נולדו מרגרסיות:**

1. ‏`SPECULAR` מוכפל ב-`near` ולא מתווסף לצידו. כשהם רק נסכמו, ‏`bright-crumb`
   ב-5254 נפל מ-`part` ל-MISS: פירור הוא גם בהיר וגם חסר-צבע. **מה שמפריד אינו
   הפיקסלים אלא הגאוגרפיה** — להבהק צריך מבנה שייצר אותו, פירור נוחת איפה שנחת.
2. הזיכוי מוכפל ב-`(1−near)`. כשהוא יכול היה לקזז את איבר הגאומטריה, ‏5235 עלתה
   מ-3 ל-4 ריפויים — "זה פיגמנט ולא הבהק" אינו תשובה ל"אתה עומד על הגבה".

**התוצאה על כל 23 התמונות** (ברירת מחדל `redness 90 / spots 25`):

| | לפני | אחרי |
|---|---|---|
| ריפויים | 87 | **58** |
| מהם על אנטומיה | 49 | **30** |
| מסגרת עין | 2 | **0** |
| ‏collateral על 5254 (סוויטת הריקול) | 718px | **15px** |

**מעקות הבטיחות, כולם נמדדו A/B באותה הרצה ולא מול מספר מהתיעוד:**
‏`marking == apply` הפרש 0 פיקסלים · ‏`test_blush` PASS על שני הכלים ·
סימונים מוזרקים: ‏4934 ‏3/5 ו-5254 ‏4/5, **זהה לבסיס**.

**מה נבדק בעין מכל מה שהכלי הפסיק למצוא** (‏`_gt_lost.py`) — תשעה ממצאים בשתי הפנים
הקשות בסט:

* ‏`1791` ‏n7/n11/n13 — הרמפה אל השפה, קמט פינת הפה, הצללה מפוזרת. **נפסלו נכון.**
* ‏`1791` ‏n4/n5/n14 — אדמומיות מפוזרת, ו**נוקתה בכל זאת** על ידי מעבר הפיגמנט.
  לא אבדו; עברו לאופרטור הנכון.
* ‏`5254` ‏n1 (פינת העין) ו-n2 (שערה חוצה) — **נפסלו נכון.**
* ‏`5254` ‏n3 — **אובדן אמיתי, אחד מתשעה.** פירור בהיר קטן על גבול השפה: בהיר,
  חסר-צבע, *וגם* צמוד למבנה — החפיפה היחידה שהצימוד לא מפריד.

**המגבלה הידועה שנשארת, ומה יפתור אותה:** להבהק ולפירור אותה חתימה צבעונית ליד
מבנה. מה שמבדיל ביניהם הוא **הגבול** — להבהק יש שיפוע רך, לפירור קצה חד. זה האיבר
הרביעי, וכשהוא ייכתב הוא צריך להיבדק על אותה סוויטה.

**כיול:** סריקת `STRUCT_LIFT` על שש תמונות הלולאה הפנימית (`_gt_fast.py`) נתנה
‏0 ⇦ 38 ריפויים, ‏2.2 ⇦ 25, ‏3.2 ⇦ 19, ‏4.2 ⇦ 18. נעצר על **2.2**: מעליו `5015`
מתחילה לאבד ממצאים שלא אומתו, והמדד עצמו נשחק — "% על אנטומיה" נמדד כמרחק
מציון-דרך, והוא סופר גם סנטר ושפה עליונה שהם עור לגיטימי. כיול מדויק יותר דורש
מדד טוב יותר, וזו משימה נפרדת ומוצהרת.

---

## גם נסגר: הכשלים השקטים (2026-08-04)

**‏0 = כבוי** (שורש 6). ‏`cleanup._scan_face` בודק עכשיו את חוגת הכתמים לפני שהוא
מודד משהו, ומסמן `scan.spots_off`. השער יושב ב**סריקה המשותפת** ולא בשני נתיבים,
כי `detect` ו-`apply` אסור שיחלקו על השאלה הזאת — זו בדיוק הסיבה שיש סריקה אחת.
מדוד על `321A5078`:

```
spots=0   candidates=0  heals=0   notes={'spotsOff': 2, 'faceTooSmall': 3}   pigmentPx=12,788
spots=25  candidates=9  heals=4                                              pigmentPx=12,788
```

כלומר החצי הנקודתי נעצר לחלוטין, ותיקון הצבע והברק ממשיכים — כיבוי ריפוי כתמים
אינו בקשה להפסיק ליישר פיגמנט.

**שער הגודל מדבר** (שורש 4א). שתי בעיות, לא אחת:

1. הנתיב של פנים בודדות לא דיווח `faces` בכלל, ולכן לחזית לא היה **מכנה**.
   עכשיו הוא מדווח.
2. וזו החמורה — ‏`explain.ts` הכריז על `faceTooSmall` כחוסם מוחלט: *"הכלי דילג
   ולא נגע בפיקסל אחד"*. על `321A5078` זה **שקר מדיד**: שלוש פנים דולגו, שתיים
   טופלו, וחמישה כתמים רופאו. חוסם שמגזים בעצמו מלמד את אותו חוסר-אמון כמו כלי
   ששותק. הנוסח עכשיו: *"3 מתוך 5 הפנים בפריים קטנות מדי — הכלי דילג עליהן ועבד
   על השאר"*.

‏`marking == apply` נבדק מחדש על אותו פריים בן חמש הפנים: **הפרש 0 פיקסלים**.
‏`tsc --noEmit` נקי.

---

## מה עוד לא נמדד, ולא אתיימר שכן

**`face-retouch` לא קיבל ציון כמותי.** אין לו רשימת ממצאים, ולכן דירוג הוגן שלו דורש
מדידה לפי אזור: על כל כתם אמיתי — כמה נחלש; על כל אזור שאינו כתם — כמה השתנה; וכמה
**היפוכים** (כתם שהתחזק), שנמדדו בעבר 26 מתוך 54 על פני אקנה אחת ‏(`TOOLS-STATUS.md`).
מה שכן ידוע מהסט הזה: הוא פעל על 11 הפנים ש-`skin-cleanup` דילג עליהן, והשינוי שלו
מתון (‏`rMax` ‏6–47 מול 0–100 של cleanup). זו העבודה הבאה על התור.
