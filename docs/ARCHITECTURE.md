# ARCHITECTURE — עורך התמונות (שם זמני: "סטודיו")

> מסמך זה הוא **מקור-האמת**. כל קוד שנכתב חייב להתאים לו. אם משהו נבנה בסתירה למסמך — או המסמך מתעדכן קודם, או הקוד לא נכנס. המסמך מתוחזק תוך כדי, לא בדיעבד.

---

## 1. מה אנחנו בונים

כלי פוסט-פרודקשן **מקומי** לצלמות (נישה: מגזר חרדי, עברית).
זרימה: מייבאים תיקייה → בונים **מתכון עריכה** (כלים גלובליים + כלי AI) על תמונת ייחוס → מחילים על כל הסשן → מייצאים. הכל מקומי, פרטי, שום תמונה לא עוזבת את המחשב.

חזון-על (North Star): "כל מה שצלמת צריכה, במקום אחד". **הטריז שבונים עכשיו: העורך.** מעצבים אותו כך שיגדל לפלטפורמה — לא בונים את הכל בבת אחת.

---

## 2. עקרונות-על (לא ניתנים למשא ומתן)

1. **Pipeline אחד, מקור-אמת אחד.** המנוע (Python) מריץ את *כל* שרשרת העריכה. `preview == export` תמיד.
2. **כל כלי הוא אזרח אחיד.** גלובלי (חשיפה) או AI (החלקת עור) — אותו `Tool` interface, אותו pipeline, אותו מתכון. אין "האק".
3. **עריכה לא-הרסנית.** מתכון = stack מסודר של שכבות. המקור אף פעם לא משתנה.
4. **עלות-החישוב על חשבון המשתמש, לא על חשבון העסק.** ברירת מחדל: עיבוד על מחשב המשתמש. **הרציונל הוא כלכלי, לא אידאולוגיית-פרטיות:** אם העסק צריך לארח שרתים שירוצו את העיבוד הכבד (AI) עבור כל צלמת ואצווה, זו עלות-תשתית שמצטברת לאינסוף. **אין פסול באינטרנט עצמו** — קריאת-רשת שהמשתמש עצמו נושא בעלותה (למשל מפתח-API אישי לשירות AI חיצוני, ששולם ישירות לספק ולא דרך העסק) עומדת באותו עיקרון בדיוק כמו הרצה מקומית, כי בשני המקרים העלות לא נופלת על העסק. מה שכן אסור: העסק מארח/ממן חישוב-כבד מרוכז בשביל המשתמשים. אין שרת-ענן בבעלות-העסק ל-v1.
5. **בונים כדי לשמור.** בלי קיצורי-דרך שקטים. פרוטוטייפ זריק — רק בהכרזה מפורשת ובאישור.
6. **רישוי מסחרי בלבד.** מודלי קוד-פתוח: MIT / Apache / BSD בלבד. אסור לא-מסחרי (CodeFormer, BRIA RMBG).

---

## 3. טופולוגיית המערכת

```
┌─────────────────────────────────────────────────────────────┐
│  Electron Shell  (EXE מותקן, Windows קודם)                   │
│  · מדליק ומנהל את המנוע כ-sidecar                             │
│  · גישה למערכת הקבצים (ייבוא/ייצוא)                          │
│                                                              │
│   ┌────────────────────────┐     ┌───────────────────────┐   │
│   │  Renderer (React + TS) │◄───►│  Engine (Python)      │   │
│   │  · UI, ערימת המתכון    │ IPC │  · מקור-האמת לעיבוד   │   │
│   │  · תצוגה מקדימה מהירה   │/HTTP│  · RAW, גלובלי, AI    │   │
│   └────────────────────────┘     └───────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
         כלום לא יוצא מהמכונה ·  localhost בלבד
```

- **Renderer** — כל ה-UX ותצוגה מקדימה *מהירה* (JS/WebGL על proxy מוקטן) לחוויית סליידר מיידית.
- **Engine** — מקור-האמת: פענוח RAW, הכלים הגלובליים ה"אמיתיים", וכל כלי ה-AI. הוא שמרנדר את הייצוא הסופי.

> **הבהרה:** "כלום לא יוצא מהמכונה" למעלה מתאר את מה שבנוי היום (v1) — לא איסור עתידי על כל קריאת-רשת. ראו עיקרון #4 לעיל: קריאה לשירות חיצוני שהמשתמש עצמו משלם עליה ישירות (לא דרך העסק) לא סותרת את העיקרון, כי העיקרון הוא כלכלי (מי נושא בעלות-החישוב), לא איסור-פרטיות גורף.

---

## 4. מודל הדומיין (Core data model)

```ts
type ToolKind = 'global' | 'ai';
type BatchPolicy = 'absolute' | 'adaptive'; // איך הכלי מתנהג באצווה

interface ToolParamSpec {
  id: string; label: string;
  min: number; max: number; step: number; default: number;
}

// מופע של כלי בתוך מתכון: איזה כלי + ההגדרות שהצלמת בחרה
interface ToolInstance {
  toolId: string;
  params: Record<string, number>;
  enabled: boolean;
}

// המתכון = ערימה מסודרת של כלים. זה הלב.
interface Recipe {
  tools: ToolInstance[];
}

// סגנון שמור בשם ("לוק ים", "TEZA Warm") — שכבה 3
interface Style {
  id: string; name: string; recipe: Recipe;
}

interface Photo {
  id: string;
  path: string;        // נתיב מקומי — המנוע קורא ישר מהדיסק
  name: string;
  recipe: Recipe;      // המתכון שהוחל על התמונה הזו
}

interface Session {   // "סשן" = תיקייה/גלריה של עבודה
  id: string; name: string;
  photos: Photo[];
}
```

---

## 4.1 הפרויקט על הדיסק, מקבצים, ושלוש שכבות המתכון

> נקבע 2026-08-04, מול המשתמש, אחרי בירור ארוך. **מחליף את סקיצת
> `Session`/`Photo` שלמעלה** — היא נשמרת כהיסטוריה, אבל אינה המודל.
> קוד: `engine/workspace.py`, `src/studio/store.ts`, `src/types.ts`.

### שתי תיקיות, בדיוק שתיים

הייבוא **מעתיק** — הוא לא מצביע. הפרויקט הוא בית, לא קיצור דרך:

```
<שורש>/<שם הפרויקט>/
  תמונות גלם/     RAW_DIR    — האמת. נכתב פעם אחת בייבוא, אף פעם לא שוב
  תמונות/         EDITED_DIR — עותק אחד עדכני לכל פריים, מוחלף בכל החלה
  project.json    STATE_FILE — הידע, נוסע עם התיקייה
```

השורש נקבע פעם אחת ויש לו ברירת מחדל (`Documents/TEZA`) — **הוא לא נשאל לפני
הייבוא הראשון.** זה נוסה ונכשל בשימוש: השאלה "איפה לשמור פרויקטים" נשאלה למי
שבא להכניס תמונות, הוא הצביע על תיקיית התמונות שלו, ונוצר שם פרויקט ריק
שהמתין לייבוא שלא הייתה סיבה לצפות לו. שאלה עם תשובה סבירה מקבלת ברירת מחדל
ומוצגת, לא נשאלת.

**כל החלפה ב-`תמונות` מחושבת מהגלם דרך המתכון המלא — לעולם לא מעל הקובץ
שכבר שם.** ערימה של כלי על גבי פלט קודם היא דור JPEG נוסף לכל כלי (תקציב
הטקסטורה של 95% לא שורד את זה) והופכת כל צעד לבלתי הפיך ברגע שרץ.

### מקבץ הוא תגית, לא תיקייה

מקבץ = **אור**. קבלת פנים, חופה, ריקודים, בחוץ. הצלם חותך את היום בבריכה
מצטמצמת: מסמן קבוצה, נותן שם, היא יוצאת מהבריכה, ממשיך על מה שנשאר.

השיוך הוא שדה פר-פריים ב-`project.json` (`assign`), **לא** תת-תיקייה. שלוש
סיבות, ואף אחת מהן אינה טעם:

1. **הגבולות לא חופפים.** כרטיס אחד מכיל את כל היום; מקבץ אחד נפרס על שני
   כרטיסים (צלם שני בריקודים). תיקייה פיזית ומקבץ לא מתלכדים אף פעם.
2. **שיוך מחדש היה הופך להעברת קובץ.** הנתיב הוא הזהות (`store.ts`,
   `Frame.path`), וכל מתכון, סטטוס ומסכה ידנית מצביעים עליו. `move` שובר את
   כולם בשקט.
3. **פריים יכול להשתייך לשניים** — פורטרט משפחתי שצולם בחוץ.

תיקיות לפי מקבץ **כן** נכתבות — במסירה, שם ממילא נוצרים קבצים חדשים. הדיסק
נראה כמו שהצלם רוצה בלי שמבנה הדיסק ינהל את המוצר.

### שלוש שכבות, וכלל אחד שקובע לאן כלי הולך

```ts
interface ProjectRecipe {
  version: number;
  base: ToolInstance[];                        // כל הסט
  perBatch: Record<string, ToolInstance[]>;    // אור אחד
  perFrame: Record<string, ToolInstance[]>;    // תמונה אחת
}
```

הדריסה לפי `toolId`, מהרחב לצר: `effectiveRecipe(projectId, path)`.

> **כלי שהמצלמה השפיעה עליו יושב במקבץ; כלי שהאדם השפיע עליו יושב בבסיס.**

| שכבה | מה יושב שם | למה |
|---|---|---|
| `base` | ניקוי כתמים, החלקת עור, חידוד, רעש | תלויי **תוכן**. אותו עור מקבל אותו טיפול בחוץ ובפנים |
| `perBatch` | צבע נלמד, איזון לבן, חשיפה, גריידינג | תלויי **אור**. גרייד אחד לכל האירוע הוא שקר |
| `perFrame` | חריגה, ומכחול/סימוני-כתמים | גיאומטריה של פנים אחת בפריים אחד לא עוברת הלאה |

הכלל הזה נותן תשובה חד-משמעית לכל כלי חדש, במקום ויכוח בכל פעם.

**רק `perFrame` רשאי לשאת מצב פר-תמונה.** `shareable()` מפשיטה `selection`
ו-`mask.paint` בדלת היחידה אל `base`/`perBatch`.

### שולחן העבודה כותב פר-תמונה

`SetWorkbench` שומר ל-`perFrame` כברירת מחדל (`setFrameStep`), ו"החל על כל
המקבץ" (`setStep`) הוא כפתור שני ומכוון. ריטוש נשפט על פנים אחת: הגדרת החלקה
שמחמיאה לפריים הזה היא ניחוש לגבי כל שאר הפנים במקבץ. היכולת לא נלקחה — היא
הפכה מהחלטה שקורית בטעות להחלטה שלוחצים עליה.

**מסך שמכוונים בו כלי חייב להציג את הפריים דרך כל הערימה שלו.** זה נשבר פעם
אחת (`activeSteps(projectId)` בלי נתיב מחזיר את הבסיס בלבד), והתוצאה הייתה
כיוונון על תמונה שהסט מעולם לא נראה כמותה. נמדד ותוקן: 147,149,143 (גלם) מול
128,132,111 (הערך המדורג).

### התאמת צבעים נעולה למקבץ

המקבץ נבחר **במסך עצמו** (שלב 1), לא נרש בשקט ממי שפתח אותו. בורר תמונת
המקור מסונן למקבץ, החלפת מקבץ מפילה את הזוג שנבחר — פריים מהגן לא יכול ללמד
את צבע רחבת הריקודים — ומקבץ שכבר יש לו מראה חוסם את הזרימה בהודעה, כי
`setStep` **מחליף ולא ערם**, ואין דרך לשחזר את מה שנדרס.

### ביצועים

הצנרת מרנדרת פעם אחת לרוחב קנוני והמסכות ממופתחות לפי **התמונה ולא לפי
המתכון**. הנימוק, המספרים והפער שנשאר: `docs/BUGS.md` ⇦ BUG-002.

---

## 5. חוזה הכלי (ה-interface האחיד)

**זה המנגנון שמונע "האקים".** כל כלי — גלובלי או AI — מממש את אותו הדבר:

```ts
interface ToolDef {
  id: string;
  label: string;              // עברית
  kind: ToolKind;
  category: ToolCategory;     // קובע סדר ב-pipeline
  params: ToolParamSpec[];
  batchPolicy: BatchPolicy;
}
```

צד-מנוע, כל כלי מספק:
```python
def analyze(image, ctx) -> dict:   # אופציונלי: הצעת ערכים / חישוב מסכה מראש
    ...
def apply(image, params, ctx) -> image:   # ההחלה עצמה
    ...
```

**סדר ה-pipeline** (לפי `category`, קבוע וצפוי):
```
raw-develop → local-ai (skin, eyes, face) → tone/color (global)
            → scene (background blur, sky) → artistic
```
### MaskProvider — הליבה של כל כלי ה-AI

**כל כלי AI במוצר הוא (מסכה + פעולה).** לכן יש מקום *אחד* שעונה "איפה נמצא X בתמונה" — `engine/masks.py`:

```python
get_mask(rgb, kind) -> float32 mask (H,W) in 0..1
# kinds: subject | hair | face-skin | body-skin | eyes | cheeks
```

מודלים: **MediaPipe** (Apache 2.0) — `selfie_multiclass_256x256` לפילוח, `face_landmarker` ל-478 נקודות. רצים מקומית, יורדים דרך `engine/setup_models.py` (לא ב-git).

**המשמעות:** כלי AI חדש = לבחור kind + לכתוב פעולת פיקסלים. **לא לחבר מודל חדש בכל פעם.**

### עומק (`engine/depth.py`) — MiDaS small (MIT) דרך onnxruntime

> **כלל:** "לחתוך את הנושא ולטשטש אחיד את כל מה שמאחור" מייצר **אפקט מדבקה** — כי בעדשה אמיתית הטשטוש **גדל עם המרחק**. לכן טשטוש הרקע מרכיב מספר רמות טשטוש לפי `|depth − subject_plane|`: הקרקע לרגלי המצולם נשארת חדה, הרקע הרחוק נמס, וגם החזית הקרובה מתרככת — כמו עומק שדה אמיתי. **בוקה** מתקבל מקרנל **עגול** + פריחת היילייטים, לא מגאוסיאן.
>
> *מגבלת זיכרון ידועה:* קומפוזיציית הרמות מחושבת על התמונה כולה — לייצוא ברזולוציה מלאה נצטרך לעבוד באריחים (tiles).

> **כלל מוצר — EXIF orientation חובה:** מצלמות שומרות צילום פורטרט כ**לרוחב + תג סיבוב**. דפדפנים מיישמים את התג, PIL **לא**. בלי `ImageOps.exif_transpose` בכל טעינה: MediaPipe מנתח תמונה שוכבת ולא מוצא פנים, והתוצאה חוזרת **מסובבת ב-90°** ביחס לתצוגה. מיושם ב-`common.load_image` / `b64_to_image`.

> **כלל מוצר — זיהוי פנים דו-שלבי:** הגלאי המובנה של MediaPipe הוא **short-range** — הוא מצפה שהפנים ימלאו חלק ניכר מהפריים, ומחזיר **ריק בשקט** לפנים שהן 10% מקובץ 3648px. זה השבית בשקט את *כל* מסכות ה-landmarks (עיניים, לחיים, תווים). הפתרון ב-`_face_landmarks`: מנסים על הפריים המלא, ואם ריק — **מאתרים פנים דרך הפילוח** (עובד בכל קנה מידה) ומריצים landmarks על **crop**, ממפים בחזרה. *(אותה תבנית שהצילה את החיתוך.)*

> **כלל מוצר — מסכת תווים מוגנים (`face-features`):** עיניים + **גבות** + שפתיים + נחיריים **אף פעם** לא עוברים ריטוש. בלי זה מחיקת "כתמים כהים" מוחקת לילדה את הגבות. כל כלי פנים חייב להחריג את המסכה הזו.

### מודל העור (`engine/skinmodel.py`) — הליבה של ריטוש הפנים

**היפוך התפיסה:** במקום לתאר *איך נראה פגם* (רשימה שרירותית שלעולם לא נגמרת — כהה, אדום, בהיר, צהוב…), בונים מודל של **מהו העור של האדם הזה**, וכל מה שהמודל לא מסביר הוא פגם — **בכל כיוון**.

**הפרדת תדרים היא מה שמציל את הסומק:**

| | קנה מידה | תדר | מה עושים |
|---|---|---|---|
| סומק, גוון אישי, צל רך | ענק | **נמוך** | `low` — **לא נגיש לכלי כלל** |
| פגם | קטן ומוגדר | **בינוני** | `mid` — כאן פועלים |
| נקבוביות, רעש | זעיר | **גבוה** | `high` — נשמר |

המודל הוא **משטח חלק** (normalized convolution על מסכת העור בלבד), ולכן הוא **מתעקל מעצמו** להיות ורוד בלחיים ובהיר במצח — **הסומק נבלע בתוכו**. זו **ערובה מבנית**: הכלי מרכיב את `low` ללא שינוי, ולכן *מתמטית* אינו יכול לנטרל צבע טבעי.

> **שקלול צבע > בהירות (`W_L=0.25, W_A=1.0, W_B=0.7`):** קמט הוא **גיאומטריה** — משנה בהירות, שומר צבע. פגם הוא **פיגמנט** — משנה צבע. בשקלול שווה הגלאי סימן את קו החיוך, קמט העפעף וקו הסנטר — וריטוש שלהם משטח את הפנים. הנמכת משקל `L` הופכת אותו כמעט עיוור למבנה ורגיש לחלוטין לסימנים. *(במדידה: 4245 → 1319 פיקסלים מתוקנים, וקו החיוך יצא מהתמונה.)*

> **ריפוי במקום מילוי:** `cv2.inpaint` *ממלא חור* מהשוליים פנימה → כתם שטוח חסר מרקם ("פלסתר"). במקומו: `low + mid*(1-conf) + high*(1-conf*0.3)` — הצבע נשמר, הפגם מוסר, **המרקם נשאר**.

> **מפת ביטחון רציפה, לא מסכה בינארית:** מסכה בינארית יוצרת קצה חד. הביטחון נגזר מהמרחק הסטטיסטי דרך smoothstep, והתיקון פרופורציונלי לו.

**רשת ביטחון:** רכיב גדול מ-16% מרוחב הפנים לעולם אינו "פגם" — הוא אופי עור שהמודל לא הצליח לבלוע. נדחה.

**`engine/test_blush.py` — assertion אוטומטי:** מודד את צבע תפוחי הלחיים לפני/אחרי בעוצמה מקסימלית ונכשל אם הוא זז מעל 0.6 יחידות Lab. *(נמדד: dL=0.004.)* **להריץ אחרי כל שינוי בגלאי.**

> **כלל מוצר — כתם הוא סטיית *צבע*, לא רק בהירות:** גלאי שבודק רק "כהה יותר" (לומיננס) **עיוור לכל סימן אדום** — וזה רוב הסימנים על פני ילד (גירוי, שריטה, שאריות אוכל). הגלאי עובד ב-**Lab**: סטייה ב-`L` (כהה) *או* ב-`a` (אדום), עם **סף אדפטיבי** לפי סטיית התקן העמידה של העור באותה תמונה — כך הכלי מתנהג זהה על עור נקי בסטודיו ועל עור גרגירי בחוץ.

> **כלל מוצר — שערי צורה חלים רק על כתמים כהים:** שערה היא חומה-כהה ומוארכת; סימן גירוי הוא **אדום** ויכול להיות מוארך גם הוא. להחיל את אותו שער "לא מוארך" על שניהם = לדחות סימנים אדומים אמיתיים. לכן: **כהה → שערי צורה מחמירים** (הגנה מפני שיער); **אדום → שערים מרופפים** (שיער אינו אדום).

> **כלל מוצר — מרווח הגנה לפי סוג התו:** לתווים עם שיער (עיניים, גבות) נדרש מרווח נדיב — ריס או שערת גבה יושבים *מחוץ* לטבעת ה-landmarks. לשפתיים ולאף **אסור** מרווח נדיב: הוא חוסם ריפוי של סימנים *לצד* הפה, שם בדיוק יושבים סימני האוכל/גירוי של ילד.

> **כלל מוצר — כתם מול שערה:** גלאי "כתם כהה קטן" תופס גם שערות פוני/תינוק. שלוש הגנות: החרגת מסכת שיער מורחבת · **פתיחה מורפולוגית** (קו דק נעלם, כתם עגול שורד) · שערי צורה (מוארכות/דלילות נדחות). על תמונת מבחן: 133 → **9** כתמים.

> **כלל מוצר — סף רזולוציה לריטוש פנים:** כלי פנים (ניקיון עור, החלקה, עיניים, סומק) **מסרבים לפעול** כשרוחב הפנים קטן מ-~180px (ניקיון) / ~120px (החלקה), ומחזירים `faceTooSmall`. הסיבה: בפנים של 70px, פצעון ונחיר הם באותו גודל — "ניקוי" מוחק תווי פנים אמיתיים. **עדיף לסרב מאשר לייצר בוץ.** ריטוש פנים אמיתי דורש פנים של 800–1500px, כלומר קובץ מקורי מהמצלמה.

> **לקח מהשטח:** `subject` אינו "כל מה שאינו רקע" — כביסה תלויה מסווגת כ"בגדים" ונכנסה בטעות. התיקון: שומרים רק רכיבים קשירים שמכילים **אדם אמיתי** (עור/שיער).

### פירוק ריטוש העור — ארבעה שלבים, פסק דין לכל אחד

אחרי שבעה ניסיונות שנכשלו כמכלול, פורק הצינור לרכיבים שנבדקים **בנפרד**. זה מה שאיתר סוף-סוף את נקודת הכשל האמיתית — קודם לכן נשפטה רק התמונה הסופית, ולכן כל סיבוב היה ניחוש.

| # | שלב | מה הוא עושה | פסק דין (נמדד) |
|---|---|---|---|
| 0 | **מסכות** | פנים · עור · שיער · **אנטומיה** | 🔴 **מסכת האנטומיה גסה מדי** |
| 1 | **מודל עור** | novelty — מה המודל לא מסביר | 🟢 עובד |
| 2 | **החלטה** | מ-novelty למסכת תיקון | 🔴 **שבור — צורת המסכה** |
| 3 | **ריפוי** | בונה מחדש את מה שמתחת למסכה | 🟢 עובד |
| 4 | שיקול דעת | שומה מול פצעון | ⚪ נדחה בכוונה |

**ראיות:** שלב 1 סימן כתם גדול **בדיוק** על השריטה — הוא ראה אותה. שלב 3 ניקה אותה לגמרי כשקיבל מסכה ידנית. בין שני שלבים עובדים, התוצאה מתפרקת ב-2.

> **הכשל המרכזי — נגע הוא *אזור*, אבל ההחלטה היא *פר-פיקסל*.** סף על מפת ביטחון מייצר **רסיסים מנוקבים**, לא גוש רציף. מדוד על אותה שריטה: מסכה ידנית = **גוש אחד, 631px** → נוקתה. מסכה אוטומטית = **100 רסיסים, 3404px** → לא זזה.
>
> וזה קטלני דווקא בגלל שהריפוי עובד: שחזור בונה מחדש מהפיקסלים **שסביב** המסכה. כשהמסכה מנוקבת, ה"סביבה" שממנה הוא דוגם **היא הנגע עצמו** — הוא משחזר את הכתם מתוך הכתם. זה גם ההסבר המאוחר לתלונה "משאיר את המבנה של הסימן": מעולם לא היה זה כשל של הריפוי.

> **שלב 0 ו"דליפת אנטומיה" הם אותה תקלה, לא שתיים.** גבול השפה, קמט הסנטר, צל הנחיר וקו הלסת סומנו כפגמים — כי `face-features` גסה מדי. זו **אינה** בעיית שיקול דעת אלא הנדסה דטרמיניסטית: ל-`face_landmarker` יש 478 נקודות שנותנות את קווי המתאר האלה במדויק. **חייב להיתקן ראשון** — לא כי הוא יפתור, אלא כי בלעדיו מפת הזיהוי מוצפת קמטים ואי אפשר להבחין בין "פספס לכלוך" לבין "סימן קמט".

> **באג — `patch_frequency` סתר את התיעוד של עצמו.** ה-docstring הבטיח "מעביר רק תדר גבוה של התורם"; הקוד חישב `donor_low + tone_delta + donor_high`, כלומר **השתיל את כל מבנה הטלאי** והמציא קמט שלא היה. הוחלף ב-`inpaint-texture`: הטון מדיפוזיה (שם היא מצוינת — מתפרה לפי התאורה סביב), והתורם תורם **אך ורק** מרקם נקבוביות (מה שדיפוזיה הורסת). מרקם נמדד בתוך התיקון מול הטבעת סביבו: Telea 0.42 · patch-frequency 0.59 *(+ארטיפקט)* · **inpaint-texture 0.68**.

> **באג — שחזור הוא הכל-או-כלום, ואסור למזג אותו בשקיפות חלקית.** מיזוג התוצאה עם המקור לפי ערך הביטחון משאיר **רפאים פרופורציונליים של בדיוק מה ששוחזר**. שחזור מוחל במלואו בתוך המסכה, עם feather צר לתפר בלבד. *(זה שונה מריפוי-תדרים, שתוכנן לביטחון רציף — שתי שיטות, שני חוזים.)*

> **הערובה המבנית לסומק משתנה במצב שחזור.** בריפוי-תדרים `low` הורכב ללא שינוי, ולכן נטרול צבע טבעי היה **בלתי אפשרי מתמטית**. שחזור בונה טון מחדש ולכן אינו נהנה מזה. מה שמחליף את הערובה: **שער הגודל** — משחזרים רק נגעים קטנים דיים שהטבעת המזינה את הדיפוזיה היא אותה חתיכת עור, כך שסומק מקומי משוחזר ולא מוחלק. `test_blush.py` הוא מה שמחזיק את זה כן.

#### מצב נוכחי — צעדים 1 ו-2 בוצעו

**צעד 1 — `face-anatomy` (`masks.py`)** ✅ מסכת הגנה מ-478 נקודות ציון, **מוחזרת כרשימת חלקים בשם** (`anatomy_parts()`) ולא כגוש: עפעפיים · אף · שפתיים · קמט אף-שפה · קמט סנטר · קו מתאר. `cleanup.py` צורך אותה במקום `face-features`.
> **עין+גבה כ-hull אחד** — קמט העפעף חי *ביניהן*, ו-hull נפרד לכל אחת השאיר בדיוק את הקמט שהגלאי סימן.
> **הפסים צרים בכוונה**, ויש assertion שמודד זאת: הסימן ליד הפה מוגן **1.2%** בלבד. פס רחב היה הופך את הסימנים שהכי צריך להסיר לבלתי-נגישים לצמיתות.
> תוצאה: גבול השפה, קמט הסנטר וצל הנחיר נעלמו מהמפה. 3404 → 2384 px. *(עדיין דולף: האוזן, ומעט מתחת לעין.)*

**צעד 2 — `cleanup.decide()` — היסטרזיס** ✅ שני ספים עם תפקידים שונים: **גבוה (0.60) מחליט *האם* זה נגע** (ליבה בטוחה), **נמוך (0.22) מחליט *עד לאן הוא מגיע***. רסיס בלי ליבה נזרק; נגע אמיתי שומר את ההילה שסף בודד חתך. ואז סגירה מורפולוגית + מילוי חורים → כל נגע יוצא **מלא**.

> **באג — השער מדד את האזור *המורחב*.** היסטרזיס הרכיבה את השריטה לנגע אחד, והשער מחק אותה **בדיוק בגלל שהורכבה**. השער נמדד מעתה על ה**ליבה**, לא על ההילה.
>
> **באג — השער מדד תיבה חוסמת.** תיבה מענישה סימן על היותו **ארוך**. נמדד: ליבת השריטה **38×50** מול שער 45 → נדחתה, כששטחה בפועל **396px בלבד**. זו **אותה טעות** ששער-הצורה-נגד-שיער כבר עשה פעם (ראה הכלל למעלה) — **אורך אינו ראיה לחפות**. השער מודד מעתה **שטח + עובי** (`distanceTransform` — העיגול הגדול ביותר שנכנס בסימן): שריטה דקה עוברת, סומק רחב נדחה.

| | התחלה | + אנטומיה | + היסטרזיס |
|---|---|---|---|
| רכיבים | 100 רסיסים | 86 | **24 נגעים מלאים** |
| זמן | 7.9s | 3.4s | **1.8s** |
| מסכה על הסימן | מנוקבת | מנוקבת | **גוש מלא** |
| `test_blush` | — | — | **PASS (dL=0.000)** |

**⚠️ פסק דין כן:** הסימן **הוחלש משמעותית — לא נעלם.** נשאר קו דק. הפנים נשארו פנים (קו חיוך, גבול שפה, סומק, מרקם — כולם שלמים, בלי מראה פלסטיק). זו הפעם הראשונה שהמסלול האוטומטי הזיז את המטרה בכלל, ואין לראות בכך פתרון.

**צעד 3 — הפרדת "מהיכן דוגמים" מ"היכן שופטים" (`skinmodel.build(support=…)`)** ✅ חלקי.

> **הכשל שנמדד — הגלאי בזבז את הביטחון שלו על שולי תווים.** `region` מנכה קמטים, פס מתאר ושוליים שחוקים — נכון לצורך *ריפוי*, אבל הם עדיין דוגמאות מצוינות לעור של האדם הזה. כשדוגמים רק בתוכו, המשטח החלק מוערך **חד-צדדית** לאורך כל גבול, וחד-צדדיות נקראת כסטייה. נמדד: novelty חציוני יורד מונוטונית עם המרחק מהגבול (1.52 ב-0-4px מול 0.60 ב-60px+), וליבות ישבו במרחק חציוני **4.4px** מגבול מול **12px** של עור טיפוסי. לכן סימן בעור פתוח כמעט לא יכול היה להתחרות. התיקון: `support = skin − face-features − hair` (בלי שחיקה, בלי קמטים) לדגימה; `region` נשאר לשיפוט.
>
> **תוצאה — שיפור אמיתי אך חלקי.** 1.52 → **1.18** בטווח 0-4px; מרחק חציוני של ליבה 4.4 → **6.0px**. `test_blush` PASS (dL=0.006). אבל **דפוס הסימון כמעט לא השתנה ויזואלית** — עדיין אוזן, מתחת לעיניים, קמט הנחיר ומסביב לשפתיים. חלק מהמדרון כנראה פיזי (שערות, צללי ריסים, גלישת גבול השפה) ולא ארטיפקט.

**צעד 5 — פירוק ה-hull של עין+גבה לשלושה חלקים לפי מה שהם באמת** ✅

> **הכשל — הגנה שנגע אמיתי אינו יכול לחמוק ממנה אינה זהירות, היא נקודה עיוורת.** המשתמש הצביע על **כתם דלקתי אדום על העפעף העליון החיצוני** (`2210-2250, 2170-2200`). נמדד: **0.0% כשיר, `conf` מקסימלי 0.00** — הצינור מעולם לא שקל אותו. והצבע מרשיע: `dL=+0.4, da=+3.0, db=+4.7` מול הטבעת סביבו — **פיגמנט, לא גיאומטריה**, בדיוק המחלקה ש-`W_A=1.0` נבנה כדי לתפוס.
>
> **שיעור מתודי:** בסבב קודם פסלתי את הפיצול על סמך מדידת "קופסת גבה" — המדד היה לא נכון, הנגע לא היה בו. **מדד שאינו מכיל את המקרה שבגללו מודדים אינו ראיה.**

> **hull לדברים שהם אזורים, רצועה לדבר שהוא קו.** ה-hull המאוחד היה נכון בכוונתו — קמט העפעף חי בין העין לגבה — אבל hull הנמתח מקו הריסים לגבה הוא **קמור**, ולכן מתנפח מעבר לזווית העין החיצונית ובולע את העפעף. זו **אותה טעות** שמסכת השפתיים כבר עשתה ("hull שמן חסם ריפוי של סימנים לצד הפה"). הפירוק: `eye-l/r` (hull + 3.0% לריסים) · `brow-l/r` (hull + 1.2%) · `eyelid-crease-l/r` — **רצועה** לאורך קו הריסים, מורמת 3.5% ובעובי 5%.
>
> כויל במדידה: הרמה 3.0%/עובי 4.0% משחרר יותר אבל **מדליף 19px לקמט** — נדחה. 3.5%/5.0% שומר את הקמט על **1px, זהה ל-hull המאוחד**, ופותח את העפעף.

| | לפני | אחרי |
|---|---|---|
| **הכתם — כשיר לטיפול** | **0.0%** | **11.8%** |
| **`conf` מקסימלי על הכתם** | **0.00** | **1.00** |
| מרופא בכתם | 0px | **156px** |
| מרופא בקמט העפעף | 1px | **1px** |
| `test_blush` | PASS | **PASS (dL=0.000)** |

**⚠️ פסק דין כן:** הכתם **הוחלש בבירור בזום — לא נעלם.** נשארה נטייה ורדרדה, בעיקר בחלק החיצוני. רק 11.8% ממנו כשיר; את השאר חוסמים `contour` (22.8%), שיער מורחב (18.9%), `face-oval` (19.4%) והשחיקה (17.2%) — ארבעה מנגנונים שונים שכל אחד מכרסם חלק.

**צעד 4 — הכלאה: `face-oval` + `infraorbital`** ✅ שני חיוביים-כוזבים גדולים חוסלו, שניהם נמצאו בביקורת נגע-אחר-נגע.

> **כלל עבודה — לבקר כל נגע בנפרד, לפני/אחרי.** עד לנקודה זו נמדד תמיד *איפה* הכלי נוגע, אף פעם לא **האם הוא צודק**; לא היה לפרויקט מספר של חיוביים-כוזבים. גיליון מגע של כל הנגעים בזום (לפני|אחרי, עם קו המסכה) חשף מיד ששני שלישים מהעבודה הולכים למקומות שגויים. זו הבדיקה שהייתה חסרה.

> **באג — `face-skin` אינו פנים.** הפילוח מחזיר גם את **האוזן, הצוואר וגלישת הלסת**. נמדד: **28% מכל פיקסל שרופא נחת שם**, על 5.6% מהשטח הכשיר — ייצוג-יתר פי 5. האוזן היא גוש רכסים וצללים ששום מודל עור לא יכול להסביר. התיקון: `region` מוגבל לתוך `face-oval` (מתאר הפנים ממולא). **`support` בכוונה אינו מוגבל** — חיתוך הדגימה באותו קו היה מחזיר בדיוק את הטיית השכנות החד-צדדית.

> **באג — החריץ התת-עיני רופא כאילו היה לכלוך.** 6 מ-22 נגעים ישבו בו ועוד 2 על קו העפעף: **30% מכל הריפוי** הפך ילדה קטנה למישהי עם עיגולים מרוטשים. שקלול הצבע אינו מסנן זאת כי החריץ אינו רק כהה — הוא **כחלחל**, סטייה אמיתית ב-`b`. התיקון: `infraorbital-l/r` — רצועה שעוקבת אחרי העפעף התחתון ויורדת `fw*0.12`. **רצועה, לא דיסקה**: הלחי שמתחתיה היא בדיוק המקום שבו יושבות השריטות האמיתיות של ילד.

| | התחלה | + support | + oval | + infraorbital |
|---|---|---|---|---|
| ריפוי מחוץ למתאר הפנים | 1175px (28%) | 1175px | **0** | **0** |
| ריפוי על מבנה העין | ~1019px (30%) | — | — | **0** |
| סה"כ פיקסלים | 4949 | 4168 | 3378 | **3265** |
| נגעים | 24 | 24 | 22 | **21** |
| מרחק ליבה מגבול ÷ עור טיפוסי | **0.37** | 0.50 | 0.62 | 0.62 |
| `test_blush` | PASS | PASS | PASS | **PASS (dL=0.000)** |

**⚠️ פסק דין כן:** שיעור החיוביים-הכוזבים ירד אך **נשאר גבוה — כ-40% בביקורת ויזואלית**. מה שנשאר: קמטי כנף-האף, קמט הסנטר, זוויות העיניים, ונגע אחד שיושב **בתוך שערות הגבה**. הפנים נשארות טבעיות ולא נפגעו, אבל הכלי עדיין עובד קשה במקומות הלא נכונים.

**הבא בתור:** הליקוי הפתוח הוא **טווח דינמי**, לא סף: סימן אמיתי בעור פתוח מגיע ל-novelty ~1.5-2, ושולי תווים ל-2.5-7. כל עוד סימנים אמיתיים לא מנצחים שוליים, כיול ספים רק מחליף חיוביים-כוזבים בשליליים-כוזבים. שתי דלתות: (א) לחסל את שארית הטיית הגבול; (ב) SAM כמקור אזורים במקום סף על מפה רציפה.

---

### פיגמנט מפושט (`engine/pigment.py`) — האופרטור השני, ומה שפתח את התמונה השנייה

> **הכשל שהוליד אותו:** על פנים עם אקנה נרחב (`istockphoto-971105428`, פנים 573px) הכלי הסיר **14%** מהסימנים, סימן בעיקר קמטים וגבות, ומרח חום. הסיבה אינה כיול: **חצי ממה שיש על הפנים אינו קבוצת נקודות בכלל.** אקנה מותיר **אדמומיות מפושטת** שנודדת על לחי שלמה — אין לה "פנים" ו"חוץ", ולכן בקשה מאופרטור שחזור להסיר אותה מחייבת אותו לבנות מחדש רבע פנים. זה המריחה.

**האופרטור עובד על `a`/`b`, ו-`L` נכתב רק בשער.** לומיננס הוא מה שהעין קוראת כמבנה, מרקם ונקבובית, ולכן עריכה שאינה נוגעת בו **אינה יכולה** לייצר פנים פלסטיק — לא כעניין של כיול. הייחוס הוא **חציון מקומי של העור של האדם עצמו**, ולכן: יחסי (זהה בכל גוון עור ובכל איזון לבן), ובלתי-אפשרי לגרירה על ידי האדמומיות שהוא מסיר.

> **כלל-על שהתגלה שלוש פעמים באותו צינור: אומד ייחוס חייב להיות סטטיסטיקה חסינה, כי הוא נאמד מדאטה שמכיל את החריגים שמחפשים.**
> 1. **`low` היה ממוצע גאוסיאני.** על אקנה צפוף המודל **הסביר את האקנה** — novelty חציוני על סימן אמיתי **1.45** מול סף **1.50**, כלומר הסימן החציוני מתחת לסף. חציון במקום ממוצע: 1.45 → **1.88**.
> 2. **`sigma` נאמד מכל העור.** פנים עם הרבה לתקן **מנפחות את הסף של עצמן** — נמדד: a ×1.19, L ×1.28. תוקן ב-sigma-clipping (`INLIER_SIGMA`).
> 3. **`knee_l` נאמד מכל העור.** 5.34 מול deficit חציוני של סימן 7.56 — נשארו 2.2 יחידות להרמה מתוך 7.6, ולכן הכתמים נשארו חומים **בלי קשר** לרוחב השער או לגובה התקרה. נאמד מעור **ללא עודף צבע**.

**שני שערים בלתי-תלויים ל-`L`, כל אחד מספיק, ואף מבנה פנים אינו עובר אף אחד מהם:**
- **שער צבע** — כהות מורמת רק אם הצבע מעיד עליה כפיגמנט. קמט הוא גיאומטריה, עודף הצבע שלו ~0 (זה בסיס `W_A=1.0`), ולכן שערו סגור ולא משנה כמה הוא כהה.
- **שער גוש** — פתיחה מורפולוגית: קו נעלם, גוש שורד. זה הכלי שכבר ביססתם נגד שיער. הוא נדרש כי **64 מ-217 סימנים כמעט חסרי צבע** (צלקות חומות שטוחות) — הם היו הנקודות שנשארו אחרי שכל האדמומיות הוסרה.

> **כלל מוצר — החרגה חייבת להתאים למה שהאופרטור מסוגל להרוס.** `face-anatomy` מונע משחזור להשטיח קמט, ונכון שכך. הורשתו לתיקון צבע עלתה הכל: נמדד, האנטומיה כיסתה **~1.00 מכל פפולה חזקה ששרדה בעוצמה מקסימלית** — כלומר בדיוק הסימנים שהכלי קיים בשבילם היו בלתי-נגישים לצמיתות. נוסף `face-pigment-protect` = אנטומיה **פחות** קמט-נאזולביאלי, קמט-סנטר ומתאר (שלושת החלקים שהם גיאומטריה טהורה). כל השאר נשמר.
>
> **מה שנוסה ונדחה:** לשחרר את החריץ התת-עיני ואת קמט העפעף לתיקון צבע. הרווח 3 סימנים מ-222; המחיר — נזק לאזור העין עלה מ-1.65 ל-2.56 בפנים של 290px. **לפנים קטנות אין מרווח:** קו הריסים והעפעף במרחק פיקסלים בודדים, ורצועה שבטוחה ב-573px אינה בטוחה ב-290px.

> **`face-oval` צר מהעור האמיתי.** הוא מתאר הלנדמרקים, ונמדד שהוא מחסיר את **רצועת המצח העליונה ואת שתי הלחיים החיצוניות** — 58k פיקסלים של עור רגיל מכוסה אקנה, ובו רוב הסימנים ששרדו. הוא קיים כדי למנוע משחזור לרפא את האוזן; לתיקון צבע אין את הכשל הזה (חריגת אוזן היא לומיננס/מבנה) ולכן הוא **מורחב ב-6%** לשלב הפיגמנט בלבד.

> **שומה אינה פגם.** היא חריגת הפיגמנט החזקה ביותר על הפנים, ולכן הדבר הראשון שגלאי novelty תופס. נמדד לפני ההגנה: שומה אמיתית **זזה 20 יחידות RGB**. `pigment.protected_spots` מחריגה אותה משיפוט ומתיקון. הסף **נמדד ולא נוחש** — deficit על עור נשפט הוא p50=0, p90=8, p99=24, והשומות בשיא 40-82; סף 13 הגן על 76 גושים (כלומר על האקנה, והאדמומיות חזרה מ-857 ל-4291px), **22** מעליו. מרווח הגידול הוא **פרמטר לפי קורא**: צר לתיקון צבע, נדיב לריפוי — לרפא הילה סביב ליבה מוגנת חורץ טבעת ונמדד כשתי סטיות הטון הגרועות בפנים.

**באג שנמצא ותוקן — `_fluid_trails` לא קיבל את הגנת האנטומיה.** הוא קיבל `face-skin * face-oval` בלי להחסיר אנטומיה, ולכן היה **המסלול היחיד שעוד יכול היה להגיע לעין**: נמדד על פנים של 290px, **כל 135 הפיקסלים שזזו ביותר מ-40 יחידות RGB** ישבו בתוך מסכת הנוזלים, על העפעף התחתון, במקום שבו `face-anatomy` שווה 1.00 — שכתוב של 111 יחידות בעין של ילדה. נוזלים זורמים מהאף והפה כלפי מטה על עור פתוח, ולכן שום דבר לגיטימי לא נאבד.

#### נמדד (`engine/test_cleanup_quality.py` — שופט בלתי-תלוי)

| | לפני | אחרי |
|---|---|---|
| **סימנים** (גלאי עצמאי, גושים קומפקטיים) | **222** | **124** (−44%) |
| **פיקסלי אדמומיות** | 13,358 | **1,561** (−88%) |
| שטח הסימנים | 27,654px | 15,284px (−45%) |
| **מרקם בתוך תיקון ÷ סביבו** | 0.71 | **1.04** (1.0 = עור אמיתי) |
| נזק לגבות · לשיער · לעיניים | 0.41 · 0.35 · 0.43 | **0.35 · 0.35 · 0.40** |
| `test_blush` | PASS | **PASS** (ילדה dL=0.027 · אקנה dL=0.114) |

> **`test_cleanup_quality.py` — השופט שהיה חסר.** כל סבב עד כה נשפט במפת הביטחון של הכלי עצמו, שאינה יכולה לומר אם הוא **צדק**. השופט החדש לא חולק שורת קוד עם `cleanup`: רקע **חציוני** (שגוש קטן אינו יכול להטות — בדיוק הכשל של שדה חלק), וסופר **סימנים** ולא פיקסלים, כי ספירת פיקסלים נשלטת על ידי מרקם עור רגיל (41k פיקסלים "כהים" על 173k של עור) ולכן כמעט אינה זזה.
>
> **שני מדדים שלי היו שגויים ותוקנו — מדד שגוי גרוע ממדד חסר.** (א) `tone_err` השווה את התוצאה ל**טבעת שלפני** התיקון, ולכן דירג תיקון צבע מוצלח כארטיפקט ושלח אותי לחפש באג שהיה האפקט המכוון. הטבעת נדגמת מהתמונה **הסופית**. (ב) `test_blush` מדד את הצבע הממוצע של תפוחי הלחיים והכשיל את הכלי (dL=1.15) על כך שהסיר אקנה מלחיים שהיו אדומות **מהאקנה**. הוא מודד מעתה רק פיקסלים **נקיים**, וכך נעשה **חמור יותר** ולא רופס: הוא תופס עכשיו כשל אמיתי ב-`abpn` (dL=5.25) שקודם עבר.

#### 🔴 רוק על השפתיים — בלתי-נגיש בהגדרה, ולא בגלל סף

נבדק על `321A1809 (2).jpg` (תינוק, פנים 249px, ארבעה מצולמים): **סרט רוק מבריק על השפה התחתונה.** הכלי לא נוגע בו — `wetTrails: 0`, `fluidTrails: 0`, שינוי על השפה **0.39 ממוצע / 2.3 מקסימום**. סבב קודם הצהיר שזה תוקן; במדידה לא השתנה כלום.

> **הכשל — הרוק אינו נגיש לאף מסלול בכלי, ושני המחסומים מבניים:**
>
> | מסלול | ההחרגה | נגישות השפה |
> |---|---|---|
> | גלאי כללי | `region = skin − face-anatomy − hair`, ו-`anatomy` כולל שפתיים | **0.000** |
> | מעבר נוזלים | `zone = reach & (orifice == 0)` — **השפתיים הן ה-`orifice`** | **מוחרג בהגדרה** |
>
> לכן שום ערך סליידר ושום כיול לא יכולים להזיז את זה. **המסקנה "הכלי חלש" שגויה — הכלי מעולם לא הסתכל.**

> **הגלאי בנוי לאובייקט אחר.** `_fluid_trails` מחפש **חוט דק שיצא מהפה ויורד על הסנטר ומסתיים בעור פתוח**. הרוק כאן הוא **ברק ספקולרי שיושב על ורמיליון השפה** — הוא לא יוצא ולא יורד. וגם אם הזון היה מרשה, שער העובי היה דוחה אותו: **10.0 מול סף 8.0.** השער נבנה כדי לדחות "כתם ברק", והרוק הזה **הוא** כתם ברק. זו אי-התאמה של **מודל**, לא של פרמטר.

> **🔴 והממצא החמור יותר — הגלאי סימן את שתי עיני התינוק כמועמדות לריפוי.** שלושת הרכיבים הבהירים הגדולים שהוא שקל היו: העין השמאלית (623px), העין הימנית (643px) והנחיר (140px). `onLip=0.00` בשלושתם. מה שהציל את העיניים הוא שער בודד:
>
> | רכיב | `projDown` | הסף |
> |---|---|---|
> | עין שמאל | **0.45** | 0.55 |
> | עין ימין | **0.45** | 0.55 |
>
> **מרווח של 0.10 בפרמטר אחד בין "בסדר" לבין מחיקת עיניים של תינוק.** ואצל הילדה השער הזה **כן נכשל** — 111 יחידות RGB על העפעף התחתון. זה אינו מנגנון בטיחות; **גלאי שמועמדיו הגדולים הם אנטומיה שגוי במודל שלו**, גם כשהשערים במקרה מחזיקים.

> **באג שהוכנס ותוקן — ההחרגה הנכונה למעבר נוזלים היא הפוכה מכל שאר המסלולים.** התיקון הראשון לבאג העין היה להחסיר `face-anatomy`, כמו בכל מסלול אחר. זה **נמדד כשגוי**: אנטומיה כוללת שפתיים, ולנוזל *כל התכלית* היא להתחיל בפה — ההחסרה הורידה את נגישות השפה ל-**0.000**, כלומר הגנה על עין במחיר הפיכת רוק לבלתי-ניתן להסרה לנצח. נוסף `face-eye-region` (עיניים · גבות · קמט עפעף · חריץ תת-עיני, ולא יותר).
>
> | | לפני התיקון | `−face-anatomy` | **`−face-eye-region`** |
> |---|---|---|---|
> | נזק לעין (ילדה, 290px) | **111.7** | 12.7 | **10.7** |
> | נגישות השפה (תינוק) | 1.000 | **0.000** | **1.000** |

#### `engine/specular.py` — האופרטור השלישי (נבנה; טרם פותר את המקרה הזה)

**מה מזהה ברק:** החזר ספקולרי מחזיר את צבע **האור**, לא של פני השטח. לכן הוא עושה שני דברים יחד — **הבהירות עולה והכרומה יורדת**. שום דבר אחר על פנים לא עושה את שניהם: פיגמנט *מעלה* כרומה, קמט *מוריד* בהירות, מרקם עור בהיר *שומר* כרומה. זו החתימה הנקייה ביותר בכל הצינור, וזה `W_A=1.0` הפוך: שם צבע זיהה סימן, כאן **צבע שעוזב** מזהה השתקפות.

> **מה שאסור לו לעשות: להפוך שפה למאט.** שפתיים מבריקות מטבען, ושפה בלי היילייט נקראת **צבועה**, לא נקייה. לכן מפת הברק **מופרדת לתדרים**: הרכיב הרחב והחלק שעוקב אחרי עיקול השפה הוא גלוס טבעי ו**מחוסר לפני כל הפחתה** — כלומר הגלוס נשמר **מבנית**, בדיוק כמו שהסומק נשמר ב-`pigment.py`. וכמו שם, האופרטור לא בונה פיקסל מחדש, ולכן הכשל הגרוע שזמין לו הוא "לא עשה מספיק".
>
> אותו אופרטור בדיוק אמור לטפל ב**עור שמנוני/מבריק** (מצח, אף) — אחד משני הדברים שריטאצ'ר עושה בכל תמונה. הוא מקבל **מסכה** ולא יודע על שפתיים כלום, כי הפיזיקה זהה.

**⚠️ פסק דין כן — התמונה הזו אינה יכולה לשפוט את האופרטור, בשום כיוון.** נמדד: 104 → 85 פיקסלים ספקולריים (18%), שיא 82 → 61 — אמיתי אבל חלקי. אבל **ב-2x, פנים שלמות, הסרט הרטוב אינו נראה גם ב-BEFORE.** כלומר בפנים של 248px הפגם הזה מתחת לרמת הנראות מלכתחילה: זו לא דוגמה נגדית וגם לא הוכחה.

> **הכלל של "לא שופטים לפי חיתוך נבחר" חל גם על מי שמחפש כשל, לא רק על מי שמכריז על הצלחה.** פסק הדין הראשון כאן ("לא מסיר את הרוק") נקבע על זום של **6.75x**, וכך גם התלונה שהתחילה את הבירור. בזום כזה הכל נראה שבור. **סבב כיול שלם רץ נגד פגם שאי אפשר לראות** — וזה בזבוז מאותו סוג בדיוק שהכלל נכתב כדי למנוע.

שני הכשלים שנמצאו בדרך הם שוב אותו חוק, ושניהם עומדים בזכות עצמם:

| # | מה היה שגוי | נמדד |
|---|---|---|
| 1 | `GLOSS_SCALE=0.30` — הפיצול ב-16px | הסרט הרטוב **רחב מזה**, ולכן סווג כגלוס טבעי ונשמר (הפחתה 0.31) |
| 2 | סף ה-`L` נאמד מכל השפה | **הברק ניפח את הסף של עצמו**: 16.9 מול שיא 82 — הפעם ה**רביעית** בצינור הזה |

> **ולמה בכל זאת אין מה לכייל כאן.** הפנים 248px, כלומר השפה התחתונה בגובה **~18px** והרוק מכסה את רובה. המפריד — "גלוס טבעי רחב, רוק יושב עליו" — דורש שהשפה תהיה **גדולה מהותית** מהכתם הרטוב, וב-18 פיקסלים אין את המרווח. אבל השורה התחתונה פשוטה יותר מזה: **בקנה מידה שבו הפגם לא נראה, אין מה למדוד.**
>
> **מה שנדרש כדי לאמת את `specular.py`: פורטרט שבו השפה גדולה דיה שהברק הרטוב נראה בגודל שיפוט** (שפה 100px+ בקירוב). בלעדיו האופרטור נשאר **בנוי, בטוח מבנית, ובלי אימות** — ואין להצהיר עליו לא כעובד ולא כשבור. *(המקרה הרלוונטי באמת הוא בכלל עור מבריק — מצח ואף — שם השטח גדול והפגם נראה גם בפנים קטנות.)*

**אומת שלא נשברה רגרסיה:** `test_blush` PASS על שתי הפנים · פני האקנה 222 → 128 סימנים, מרקם 1.08, נזק גבות 0.39.

#### חוזה הכלי — סליידר אחד לא יכול היה לעבוד

> **כלל מוצר — שני אופרטורים עם פרופיל סיכון הפוך חייבים שני פקדים.** תיקון פיגמנט אינו כותב מבנה ולכן בטוח ב-100; שחזור נקודתי סובל מפס-התדר החסר ולכן יכול להיקרא כטלאי. סליידר אחד אין לו הגדרה טובה: בברירת המחדל שנשלחה (60) הוא **חסם את החצי הבטוח כדי לרסן את המסוכן**, וזרק יותר מחצי מהשיפור (סימנים −17% מול −44%). נמדד גם החלוקה בפועל: **`redness` הסיר 85 מ-86 הסימנים; `spots` הסיר אחד** ונשא את כל הסיכון. לכן `redness=90`, `spots=25`.
>
> **התקרה 90 ולא 100 — נמדד:** 100 מנצח בפנים של 573px (132 סימנים מול 149) אבל **מפסיד** ב-290px (27 מול 24) — בקנה מידה קטן הרמת הלומיננס האגרסיבית משאירה שארית משל עצמה. רוחב הפנים בפריים אמיתי אינו בשליטת הצלמת, ולכן ברירת המחדל יושבת במקום שלא יכול להזיק למקרה הקטן.

> **באג ארכיטקטוני — חוזה הכלי מוצהר בשני מקומות ונפרד בשקט.** `src/toolRegistry.ts` ו-`engine/server.py` מגדירים שניהם את הפרמטרים. השרת המשיך להצהיר `disabled: true` וסליידר בודד ב-60 זמן רב אחרי שהמנוע יכול היה יותר — **זו הסיבה שכלי עובד נראה שבור מהצד הקדמי.** זו אותה מחלקת בעיה שכבר תועדה ב-§8 עבור מימוש הכלים הגלובליים (JS מול Python), רק שכאן זה ה**חוזה** ולא המימוש. **לשנות את שניהם יחד**, ובהמשך למשוך את הרישום למקור אחד.

> **מלכודת הפעלה — המנוע הוא sidecar, והוא לא נטען מחדש לבד.** `pigment.py` הוא מודול חדש; תהליך מנוע שרץ מלפני השינוי ימשיך להריץ את הקוד הישן **בשקט ובלי שגיאה**. אחרי כל שינוי במנוע — לאתחל אותו. וכן: `FAST_PREVIEW=1600` בלאב מוריד פנים של 3648px ל-~85px, מתחת ל-`MIN_FACE_PX`, והכלי מחזיר `faceTooSmall` ולא נוגע בכלום.

**⚠️ פסק דין כן:** מה שנשאר הן **4-6 הפפולות המורמות החזקות**. אלה אובייקטים דיסקרטיים עם תבליט — כלומר בדיוק המחלקה שמסלול ה**ריפוי** אמור לטפל בה, ו-recall שלו על התמונה הזו הוא ~30%. הלחיים, המצח, האף, סביב הפה והאוזן נקיים; גבות, מרקם, שיער וזיפים שלמים.

**צעד 6 — סבב ראיות מלא: הסרת הסימטריה, שער-מבנה, מודל דו-מעברי** ✅ (2026-07)

> **באג — ריפוי-הסימטריה היה מקור כתמי ה"בוץ".** אבחון שלב-אחר-שלב (מקור | telea | inpaint_texture | סימטריה | הרמוניזציה) הראה ש-telea+טקסטורה מרפאים **נקי**, ושלב העתקת-הצד-הנגדי הוא שמדביק טלאים שטוחים בגוון שגוי — תאורת פנים אף פעם לא סימטרית, וליד העין הוא העתיק את **ריסי העין השנייה** בתור עור. הוסר כליל (`symmetric_reconstruct` נמחק); הריפוי הוא `inpaint_texture` + הרמוניזציה בלבד.

> **שער-מבנה (`_structure_gate`) — נגע הוא מבודד, בשני מרחבים.** (א) **המשכיות-קו**: שערה תועה, זנב אייליינר וקמט הם קווים; הגלאי תופס רק את הקטע העבה, וריפוי קטע של קו "שובר" אותו באוויר. רכסי black/top-hat על L/a/b בהיסטרזיס (סף גבוה="יש קו", נמוך="עד לאן") — מבנה שנמשך מעבר לרכיב ⇒ דילוג. (ב) **המשכיות-novelty**: הצללה רכה חוצה את הסף בלי גבול מקומי; הטבעת מחוץ למסכה של סימן אמיתי היא עור מוסבר, ושל הצללה — עדיין חריגה (חציון, לא ממוצע — כתם שכן מרים חלק מהטבעת, גרדיאנט מרים את כולה). אותו מדד גם **מגדיל** רכיב שאושר עד היכן שהחריגה נגמרת (עם תקרה) — כך חצאית רכה של מריחה נרפאת ולא נשארת כעותק חיוור.

> **באג — השער נמדד אחרי סגירה מורפולוגית.** ה-close מיזג שריטה עם שברי-שערה שכנים לרכיב אחד, ווטו של השבר פסל את השריטה. `hysteresis_core()` פוצל מ-`decide()` והשער רץ על רכיבים **הדוקים**, לפני מיזוג. **באג נלווה:** מבנה שרק **נוגע** בקצה ה-reach פסל רכיב שלם — נדרשת מעורבות מינימלית (`inside ≥ max(6, 5% שטח)`).

> **מודל דו-מעברי — כתם רחב מסביר את עצמו.** השדה החלק נבנה עם הכתם בתוכו, מתכופף אליו, והחצאית יוצאת "מוסברת". מעבר שני בונה את המודל בלי הפיקסלים החשודים ומנקד מחדש — אבל **רק בשכונת חשד ממעבר ראשון**: ניקוד-מחדש גלובלי עם המודל החד הציף זיהויים גבוליים חדשים והכפיל את הווטואים.

> **רצועת גב-האף כאנטומיה (`nose-bridge` ב-`masks.py`).** הצללת צלע-האף חזרה ונתפסה בכל גרסת שער — עד שה"ריפוי" שלה שיטח את האף. מיקום הגשר **ידוע** מ-landmarks (nasion→tip, עובי 14% fw); מגנים עליו כגיאומטריה במקום להתווכח איתו כסטטיסטיקה.

> **מבחן ריקול עם אמת-קרקע (`test_cleanup_recall.py`).** `test_blush` שומר את צד השליליים; לא היה כלום על צד החיוביים — כלי שלא מזיק ולא מרפא עובר הכל. מטביעים 5 סימנים סינתטיים (מריחת לכלוך, פצעון אדום, נקודה כהה, פירור בהיר, שריטה) על עור כשיר, ומודדים שחזור מול הפיקסלים האמיתיים + collateral מול ריצת-בסיס נקייה.

| | לפני הסבב | אחרי |
|---|---|---|
| מריחת אייליינר בזווית העין | ✗ נמרחה לכתם כהה | **לא נוגעים** (וטו-קו) |
| שערה חוצת-לחי | ✗ "נשברה" באמצע | **לא נוגעים** (וטו-קו) |
| טלאים אפורים על גשר האף | ✗ | **אין** (סימטריה הוסרה; גשר מוגן) |
| ריקול מוזרק (5115 / 5254) | — | **5/5 · 4/5** (שריד 1.4–5.7 מתוך 10–22) |
| `test_blush` | PASS | **PASS (dL=0.036/0.042)** |
| פנים נקיות (5089/5290) | נזק בכל תמונה | **47/132px שינוי, אפס נזק נראה** |

> **אזהרה — הכל נמדד על תמונה אחת, והיא לא מייצגת.** הפנים ב-`321A4093` הן **293px רוחב** (landmarks) / 365px bbox, בפריים של 3648px — תמונת גוף מלא, לא פורטרט. התיעוד למעלה קובע שריטוש פנים אמיתי דורש 800–1500px, ו-`MIN_FACE_PX=180` מעביר את זה בשקט. **החלטת מוצר: הכלי חייב לעבוד גם על פנים קטנות** — כלומר זו אינה עילה לפסול את התמונה, אבל כן עילה לא להסיק ממנה על תמונות פורטרט. דרושות 3-4 תמונות אמיתיות נוספות.

> **כלל עבודה — כל שלב מוציא תוצר בדיקה משלו.** מסכת האזור · מפת ה-novelty · מסכת ההחלטה · מבחן הריפוי על מסכה ידנית. כך רואים **איזה שלב זז ואיזה נשבר**, במקום לשפוט את התמונה הסופית ולנחש. היעדר הכלל הזה עלה שישה סיבובים.

> **כלל עבודה — לא שופטים ריטוש לפי חיתוך נבחר.** חיתוך של 40px בזום 11x הוא בחירה נוחה, לא ראיה. כל טענה על ריטוש מוצגת על **כל הפנים**, בגודל שאפשר לשפוט, לצד **מפת מה שסומן** — אחרת "עובד" ו"לא נגע בכלום" נראים זהים.

> **החלטה:** ריטוש-AI רץ *לפני* הגראפ הגלובלי. נימוק כפול: (א) איכות — מריטשים על דאטה נייטרלי ואז מגריידים מעל; (ב) ביצועים — תוצאת ה-AI מחושבת מהמקור ונשמרת ב-cache, והכלים הגלובליים רצים חי מעליה ב-JS בלי להריץ AI מחדש בכל תזוזת סליידר.

יש **Tool Registry** אחד; ה-UI נבנה דינמית מהרישום. כלי חדש = הוספה לרישום, בלי לגעת בשאר.

---

## 6. ה-Pipeline (איך מתכון מרונדר)

`render(photo)` = החלה בסדר של כל `recipe.tools` דרך `apply()`.

שני מסלולי רינדור, אבל **מקור-אמת אחד**:
- **תצוגה מקדימה מהירה** (renderer): כלים גלובליים ב-JS/WebGL על proxy מוקטן → משוב סליידר מיידי. תוצאות AI מגיעות מהמנוע ו**נשמרות ב-cache**.
- **רינדור-אמת** (engine): כל השרשרת (גלובלי + AI) ברזולוציה מלאה — לייצוא ולתצוגת AI. זה מבטיח `preview == export`.

**Cache** — פעולות AI יקרות. מאחסנים פלט לפי `(photo, toolInstance.params)` כדי לא לחשב מחדש בכל רינדור.

---

## 7. חוזה המנוע (Sidecar API)

```
GET  /health                      → { status, tools[] }
GET  /tools                       → רישום הכלים (id, kind, params, category)
POST /tools/{id}/apply            → { imagePath|image, params } → image  (כלי בודד, לתצוגה/cache)
POST /render                      → { imagePath, recipe, size } → image  (רינדור-אמת מלא)
POST /analyze                     → { imagePath } → הצעות (auto-style, מאפיינים שזוהו)
POST /decode                      → { path | image, maxDim } → תצוגת JPEG   (פענוח RAW)
POST /cleanup/detect              → { image|path, params, recipe } → { items[] }  (גיאומטריה בלבד)
POST /export                      → { photos[], recipe|perPhoto, format, dest }  (עתידי)
```

**‏`/cleanup/detect` מחזיר גיאומטריה, לא תמונה** — וזו לא אופטימיזציה אלא הדרישה
עצמה. כל מועמד חוזר כקונטור מדויק מנורמל לפריים (‏0..1) עם ‏`kind`, ‏`verdict`
והמספרים שהכריעו, כי הצלמת צריכה **ללחוץ** על מוקד ולהחליט לגביו: אף אחד לא יכול
ללחוץ על פיקסל בתוך *תמונה של* לחיצה. הנרמול לפריים הוא מה שמאפשר לסמן על תצוגה
ולהחיל על הקובץ המלא — הגרסה היחידה שנמסרת בסוף. אותם קונטורים חוזרים למנוע
ב-`selection` שעל פריט המרשם, ומרנדרים דרך `drawContours(FILLED)` — אותה פעולה
ש-`_fill_holes` בונה בה כל מסכת תיקון, ולכן ההחזרה היא זהות ולא קירוב.

**החלטת build-to-keep:** תמונות עוברות ב**נתיב-קובץ מקומי**, לא base64. המנוע קורא מהדיסק (מהיר, חובה ל-RAW/תמונות גדולות). *(ה-base64 ב-POC היה קיצור-דרך — מוחלף כאן.)*

---

## 8. אסטרטגיית תצוגה מקדימה + סיכון הביצועים ⚠️

**הסיכון האמיתי:** להריץ הכל דרך המנוע גם לתצוגה חיה ← עלול להיות איטי (שנייה לכל תזוזת סליידר = חוויה גרועה).

**המיטיגציה (מובנית בארכיטקטורה):**
- כלים גלובליים → תצוגה מקדימה ב-JS/WebGL על proxy, מיידי.
- כלי AI → נחשבים במנוע פעם אחת, נשמרים ב-cache, ומוצגים כשכבה.
- רינדור-אמת מלא רק בייצוא / כשצריך דיוק.

**נמדוד את זמן הרינדור מוקדם (Phase 1), לא נגלה בסוף.** ✅ נמדד: AI ~210–244ms; רינדור גלובלי מוצג ב-UI (`Xms` ליד שם הקובץ).

> **החלטה מוצהרת — כפילות מבוקרת:** הכלים הגלובליים ממומשים ב-**JS** (תצוגה חיה מיידית — דרישת מוצר) ויְמומשו גם ב-**Python** עבור **ייצוא ואצווה** (שם המנוע חייב לרנדר מאות תמונות ברקע). זו כפילות מכוונת ומוגבלת לסט קטן ומוגדר. **מיטיגציה לדריפט: מבחן parity** (תמונת fixture → השוואת פלט JS מול Python בסבולת) — נכנס יחד עם הייצוא ב-Phase 2. זה לא קיצור-דרך שקט; זה תיעוד של הכרעה.

**מבחן ה-parity קיים (2026-07-28): `engine/test_parity.py`.** esbuild אורז את `_parity_driver.ts`, node מריץ את `applyGlobalTool` על dump גולמי של RGBA, ו-Python משווה מול המראה שלו פיקסל-פיקסל. סבולת פר-כלי: 1 רמה (פער קבוע — Python חותך לבייט, JS כותב ל-`Uint8ClampedArray` שמעגל), 2 לוינייטה (המנוע בונה את הנפילה ב-256px ומגדיל). **נכון ל-2026-07-28 `KNOWN` ריק — כל הכלים הגלובליים מסונכרנים.** מה שלא ירוק נרשם שם עם המספר במקום להיכשל, כי חבילת-מבחנים שתמיד אדומה מפסיקים לקרוא.

הרצה: `python test_parity.py <image> [--tool ID] [--sheet DIR]`. **כל שינוי מתמטיקה בכלי גלובלי חייב לעבור דרכו.**

---

## 9. מתכון · סגנון · אצווה · ייצוא (שכבה 3 — מה שהכי חשוב לצלמת)

- **בונה מתכון:** עורכת תמונת ייחוס → ערימת כלים דינמית.
- **סגנון בשם:** שומרת את המתכון כ-`Style` ("לוק ים"). שימוש חוזר בין סשנים.
- **אצווה:** מריצים את אותו מתכון על N תמונות דרך המנוע. לכל כלי `batchPolicy`: יצירתי=`absolute`, חשיפה/WB=`adaptive` (עקביות אמיתית).
- **ייצוא:** רינדור-אמת לדיסק (JPEG ל-v1; PSD/דפוס בהמשך).

הכל "בחינם" כי `Recipe` הוא אובייקט serializable אחד נקי.

---

## 10. קטלוג הכלים (roadmap + מודל קוד-פתוח + רישיון)

| כלי | Tier | מודל קוד-פתוח | רישיון | סטטוס |
|---|---|---|---|---|
| טון וצבע (חשיפה, ניגודיות, היילייטים/לבנים, צלליות/שחורים, WB, רוויה) | 1 | אלגוריתם עצמי | — | ✅ בנוי |
| תלת מימדיות (ניגודיות מקומית + וינייטה) | 1 | אלגוריתם עצמי | — | ✅ בנוי |
| צבעוניות (split-toning + דהייה) | 1 | אלגוריתם עצמי | — | ✅ בנוי |
| נקודת אור טבעית | 1 | אלגוריתם עצמי | — | ✅ בנוי |
| גלואו | 1 | אלגוריתם עצמי | — | ✅ בנוי |
| אפקט ציור שמן | 1 | JS: Kuwahara אמיתי · **Python: `cv2.xphoto` — מחזיר טשטוש** | — | 🔴 **שבור** (ראה §10.2) |
| חידוד | 1 | unsharp mask | — | ✅ בנוי |
| החלקת עור | 2 | מסכה: BiSeNet (עתידי) / YCbCr (POC) | MIT | 🟢 POC בנוי |
| טשטוש רקע / בוקה | 2 | MediaPipe selfie-multiclass **+ MiDaS small (עומק)** | Apache 2.0 / MIT | ✅ **בנוי — מבוסס עומק** |
| **סומק** (`blush.py`) | 2 | MediaPipe FaceMesh — פס על עצם הלחי | Apache | ✅ **בנוי** |
| **ברק בעיניים** (`eyes.py`) | 2 | MediaPipe FaceMesh — נקודות אישון 468-477 | Apache | ✅ **בנוי** |
| **גוונים בשיער** (`hairtone.py`) | 2 | MediaPipe selfie-multiclass (מחלקת שיער) | Apache 2.0 | ✅ **בנוי** |
| הורדת פצעונים / אובייקטים | 3 | LaMa (inpainting) | Apache | ⏳ |
| חידוד/שחזור פנים | 3 | GFPGAN | Apache | ⏳ |
| החלפת שמיים | 3 | SkyAR | לבדוק | ⏳ |
| פענוח RAW | 1 | rawpy (LibRaw) | MIT/LGPL | 🟡 מנוע מוכן (`/decode`); חיבור ל-UI מחכה ל-Electron |
| ניקיון עור — **אדמומיות וכתמי צבע** | 2 | `pigment.py` — אלגוריתם עצמי (Lab, ייחוס חציוני מקומי) | — | ✅ **בנוי ודלוק** (ברירת מחדל 90) |
| ניקיון עור — **ניקוי נקודתי** | 2 | face-skin mask + `cv2.inpaint` + תורם מרקם | Apache/BSD | 🟡 בנוי, recall ~30% (ברירת מחדל 25) |
| קאלינג (עיניים עצומות/פוקוס/כפולים) | — | MediaPipe/OpenCV/imagehash | Apache/BSD | 🔮 מודול עתידי |
| ❌ להימנע | — | CodeFormer / BRIA RMBG | לא-מסחרי | 🚫 |

---

## 9.1 ביצועים — כללים שנלמדו במדידה

הורדנו מ-**32s** ל-**12.6s** לתמונת 20MP. ארבעה כללים, כולם מדידים:

1. **אל תסריאליזו בין כלים.** כל כלי חושף `apply(rgb, params) -> (rgb, meta)`; ה-base64 הוא רק חוזה ה-HTTP. קידוד PNG של 20MP בין כל שני כלים עלה יותר מהכלים עצמם.
2. **מסכות פעם אחת מהמקור.** `masks.set_source()` — הן מתארות *איפה דברים נמצאים*, וריטוש לא מזיז אותם. פלוס cache.
3. **כלי פנים חותכים לאזור הפנים ועובדים ברזולוציה מלאה.** הקטנה כללית פוגעת בזיהוי כתמים (9→3); crop נותן גם מהירות וגם איכות. *(החלקת עור: 4517ms → 245ms.)*
4. **פונקציה סקלרית = טבלת חיפוש.** חשיפה/WB/shoulder/קונטרסט הם פונקציה פר-ערוץ → מחושבים על **256 ערכים** ולא על 60 מיליון. אזורי טונים → LUT על לומיננס. נפילות מרחביות חלקות (וינייטה, נקודת אור) → נבנות מוקטן ומוגדלות.

## 9.2 איכות ייצוא — נמדד, לא משוער

**איכות מסירה היא לא מקום לחסוך בייטים.** נמדד על קובץ מצלמה 20MP (אותה תמונה, בלי כלים):

| הגדרה | גודל | PSNR | סטייה מקס' |
|---|---|---|---|
| q94 4:4:4 | 4.6 MB | 47.5 dB | 15 רמות |
| **q97 4:4:4 (ברירת מחדל)** | 6.6 MB | 51.3 dB | 13 |
| q100 4:4:4 | 12.5 MB | 56.1 dB | 4 |
| TIFF/PNG | 20+ MB | ∞ | 0 |

**ברירת מחדל: JPEG q97, `subsampling=0` (בלי דילול כרומה), progressive, עם פרופיל ICC.** TIFF (LZW) ו-PNG זמינים כמאסטר ללא אובדן.

> **הבהרה חשובה:** קובץ מיוצא *קטן* מהמקור אינו בהכרח עדות לאובדן איכות. רקע מטושטש נדחס כמעט לאפס — במדידה, ירידה מ-6.2MB ל-2.3MB נבעה ברובה מהטשטוש, לא מהדחיסה. תמיד למדוד לפני שמסיקים.

## 10.1 תקן מקצועי לכל כלי (ביקורת איכות)

> **כלל עבודה:** לכל כלי שואלים "האם הוא מבצע את הרעיון **בצורה מקצועית**?" ואם לא — עוצרים ובונים מחדש. המוצר נמדד בפרטים הקטנים.

| כלי | הגישה הנאיבית (נפסלה) | התקן המקצועי שמיושם |
|---|---|---|
| **חיתוך נושא** | מסכה בינארית 256×256 מנופחת → שיער כמו קרטון | `matting.py`: **confidence masks רכות** + **מעבר שני על crop של הנושא** (פי-3–4 רזולוציית קצה) + ליטוש מול התמונה (guided filter) + עקומת הקשחה לליבה |
| **טשטוש רקע** | חיתוך + טשטוש אחיד → אפקט מדבקה | **מפת עומק** (MiDaS) → רדיוס פר-פיקסל לפי `\|depth − subject_plane\|`; קרנל **עגול** + פריחת היילייטים |
| **טון וצבע** | הכפלת ערכי sRGB → הסטות גוון, שריפה חדה | **חשיפה ואיזון-לבן באור לינארי** + **shoulder רך** להיילייטים + עקומת קונטרסט S (לא סקיילינג לינארי) + מסכות טונליות smoothstep |
| **חידוד** | unsharp על RGB → פרנג'ים צבעוניים | על **לומיננס בלבד** + הגבלת delta נגד הילות |
| **תלת מימדיות** | unsharp ברדיוס גדול → הילות | לומיננס + הגבלת delta + וינייטה עם falloff חלק |
| **גלואו** | טשטוש כל התמונה ו-screen → אובך | פריחה **רק מההיילייטים** (smoothstep 0.55–1.0) |
| **ציור שמן** | blur + posterize | **Kuwahara אמיתי** דרך integral images — O(n), הרדיוס לא עולה כלום *(מיושם ב-JS בלבד; Python חזר בשקט לגישה הנפסלת — §10.2)* |
| **נקודת אור** | חיבור אדיטיבי → שטיחה | **screen blend** עם falloff חלק |
| **החלקת עור** | מסכת צבע YCbCr → מרחה חצי תמונה · ואז bilateral → מוחק נקבוביות | **frequency separation**: מחליקים רק את שכבת ה**טון**, שכבת ה**מרקם** חוזרת שלמה |
| **ניקיון עור** | סף כתמים גורף → מוחק נחיריים | סף שטח יחסי לפנים + **סף רזולוציה** (`faceTooSmall`) |

## 10.2 ✅ ציור שמן — היה שבור בשלושה מקומות; **תוקן 2026-07-29**

> **סטטוס: סגור.** שלושת הכשלים תוקנו יחד, כי כל תיקון בנפרד היה מסתיר את
> האחרים. האבחון המקורי נשאר למטה — הוא הראיה שהמדידה היא שתפסה את הדריפט.
>
> **מה נבנה:**
> 1. **‏Kuwahara אמיתי ב-Python** (`globals_py._kuwahara_block`): כל פיקסל
>    לוקח את ממוצע הצבע של הרביע שלו בעל **שונות הלומה הנמוכה ביותר**. אזור
>    שטוח משתטח עוד; קצה שורד כי הרביע שלא חוצה אותו תמיד מנצח. מיושם
>    ב-`cv2.boxFilter` עם עוגן בפינה — ‏O(1) לפיקסל, כך שגודל המכחול **לא
>    עולה כלום**, וזה מה שמאפשר רדיוס יחסי-לפריים במקום תקרה של 10px.
>    ‏`cv2.xphoto` נזרק. אומת מול תעתיק מילולי של אלגוריתם ה-JS: **הפרש 0.0000**.
> 2. **נוסחת רדיוס אחת לשני הצדדים** — ‏`oil_radius_px()` ב-Python ו-
>    ‏`OIL_MIN_FRAC/OIL_SPAN_FRAC` ב-`imageEngine.ts`: ‎0.3%–3.0%‎ מהצלע הארוכה.
>    על תצוגה של 1400px: ‏16px בסליידר 30, ‏42px ב-100 (במקום 3px קבוע).
> 3. **‏`oil-paint` נכנס ל-`test_parity.py`** — 4 מקרים, סובלנות 1 כמו כל כלי אחר.
>
> **הממצא שדרש את הליטוש האחרון:** בהרצה ראשונה ה-parity נכשל — ‏max 45 רמות
> על 150ppm. **בחירת רביע היא argmin, כלומר לא-רציפה**: איפה ששני אזורים
> שטוחים נפגשים, שני רביעים יושבים כל אחד בתוך אחד מהם עם שונות כמעט זהה אבל
> ממוצעי צבע **שונים מאוד** — והפרש עיגול של ‎1e-6‎ מזיז את הפיקסל בעשרות רמות.
> ‏OpenCV צבר ב-float32, ה-JS ב-Float64Array. מעבר לצבירת float64 בלומה (הצבע
> נשאר float32 — שגיאה שם רק מתעגלת, היא לא יכולה להפוך החלטה) הוריד את זה
> ל-**max 1, ‏0ppm**.
>
> **מדידות אחרי:** קצוות חזקים נשמרים 82% ב-r=30 (הישן: **46%** — טשטוש הורס
> קצוות); מיקרו-פרט 82% (הישן: 30%); קצה חד סינתטי נשמר 140 רמות מלאות;
> הסליידר סוף-סוף משנה את התוצאה (91 רמות בין 30 ל-100). ‏10.3s ל-20MP בסליידר
> 30, ‏13.5s ב-100 — אפקט אמנותי בייצוא, לא בתצוגה. גיליון: `test-results/oil-paint/`.
> **פתוח לטעם:** הקצה העליון של הסליידר (3%) אגרסיבי מאוד על פורטרט — טווח,
> לא באג; אם יתברר שאף אחד לא משתמש מעל 60, לצמצם את `OIL_SPAN_FRAC`.

### האבחון המקורי (נשמר)

> **למה זה כאן ולא בתור באג בודד:** זה המקרה שמראה *איך* דריפט נכנס בשקט. שלושה כשלים נפרדים, כל אחד מהם לבדו מסביר "הכלי לא עובד", ואף אחד מהם לא היה נראה בקוד — רק במדידה.

**1. צד Python אינו ציור שמן. הוא טשטוש.**
`cv2.xphoto.oilPainting` ב-OpenCV **5.0** מחזיר החלקה. הראיה היא ש-`dynRatio` — הפרמטר שכל האפקט תלוי בו, זה שמקוונטט את הצבע לרמות בודדות — **חסר השפעה מוחלטת**:

| `dynRatio` | 1 | 2 | 4 | 8 | 16 |
|---|---|---|---|---|---|
| `mean\|delta\|` | 1.66 | 1.66 | 1.66 | 1.66 | 1.66 |

ובזום, r=40 הוא גאוסיאן רך. ציור שמן אמיתי מייצר **כתמים שטוחים עם קצוות חדים**. ה-docstring ב-`globals_py.py:10` מבטיח *"oil paint is a real Kuwahara, not a blur"* — **הקוד סותר את התיעוד של עצמו**, וזו בדיוק הגישה ש-§10.1 פוסלת.

**2. המכחול חסום ב-10px — ולכן אין שום מקום בסליידר שמייצר אפקט נראה.**
`r = min(10, _radius_px(...))`. נמדד: סליידר 30 → 5px · 60 → 8px · **100 → 10px**. מכחול של 10px על פריים של 2048px הוא בלתי-נראה; על מאסטר של 20MP הוא שום דבר. זו **תקרה, לא כיול** — הסליידר לא יכול להגיע לתוצאה בשום ערך.

**3. `preview != export`, ולא בקטן — האזהרה של §8 התממשה.**

| | אלגוריתם | רדיוס |
|---|---|---|
| **JS** (`imageEngine.ts:927`) | Kuwahara אמיתי | **אבסולוטי** 1-10px |
| **Python** (`globals_py.py:379`) | טשטוש | יחסי לפריים, ואז חסום |

`oilPaint` הוא גם **הכלי היחיד ב-`imageEngine.ts` שלא מקבל רדיוס יחסי** — כל השאר מכפילים ב-`Math.max(w, h)` (שורות 209, 447, 865). כלומר הוא חריג גם בתוך צד ה-JS.

> **הלקח המערכתי:** §8 הצהירה על הכפילות JS/Python כ"מבוקרת" והבטיחה **מבחן parity** כמיטיגציה. המבחן לא נכתב, ולכן שני הצדדים התפצלו לשני **אלגוריתמים שונים** ולא רק לערכים שונים. כפילות בלי מבחן אינה מבוקרת — היא רק לא נמדדת.

**התיקון — ✅ בוצע 2026-07-29 בדיוק לפי התוכנית הזאת.** ראו הבלוק בראש §10.2.

---

## 10.3 `engine/shape.py` — מדידת צורה אחת נכונה, במקום חמש שגויות

> **הבאג שחזר חמש פעמים.** כל שער בצינור שואל בסוף אותו דבר: *כמה עבה זה* ו*כמה ארוך זה*. התשובה חושבה **חמש פעמים בנפרד, בשלוש שיטות**, ובכל פעם שהיא הייתה שגויה היא הרגה זיהוי אמיתי:
>
> | # | איפה | מה קרה |
> |---|---|---|
> | 1 | שער גודל הנגע | תיבה חוסמת דחתה שריטה 38×50 ששטחה 396px |
> | 2 | שער הצורה נגד שיער | אותה טעות, מוקדם יותר |
> | 3 | וטו הקמט | נזקק לאורכיות והמציא לעצמו מדד |
> | 4 | **שער הנוזלים** | `area / max(box_w, box_h)` דחה את **חוט הריר היחיד שאומת בפרויקט**, פעמיים |
> | 5 | **`protected_spots`** | שומה **מוטה** נמדדה כמוארכת ואיבדה את ההגנה שלה |
>
> **השורש הוא התיבה החוסמת, וזו אינה בעיית כיול:** **אובייקט אלכסוני או מעוקל אינו נכנס לתיבה שלו.** חוט ריר יורד וגם הצידה, ולכן `max(box_w, box_h)` **קצר** מהחוט, ו-`area/that` מדווח על הדבר **הדק ביותר על הפנים** כרחב מדי. ככל שהחוט יותר "חוט" — כך הטעות גדלה.
>
> **הראיה, על שלושה רסיסים של אותו חוט:**
>
> | רסיס | האומדן הישן (תיבה) | `thickness_typ` |
> |---|---|---|
> | 1 | 5.4 | **5.6** |
> | 6 | 12.1 | **5.6** |
> | 15 *(הריר)* | 8.2 | **5.6** |
>
> אותו רוחב פיזי בדיוק; התיבה דיווחה שלושה ערכים לפי הזווית בלבד. הסף 7.8 — הריר נדחה **במרווח 0.4px**, וכעת עובר במרווח של 28%.

**המדידה הנכונה — התמרת מרחק, שאינה יודעת דבר על צירי התמונה:**

```
thickness_typ = 4 × median(DT)    ← למוט ברוחב w ה-DT אחיד על [0,w/2], והחציון בדיוק w/4
thickness_max = 2 × max(DT)
length        = area / thickness_typ
elongation    = length / thickness_typ
```

> **`thickness_typ` ל"האם זה דק?"** — נוזל תלוי **מסתיים בטיפה**, וחציון מתעלם מהטיפה בעוד שמקסימום נשלט על ידה. נמדד סינתטית: חוט עם טיפה → `thick~8.8`, `max22.0`. **`thickness_max` ל"האם יש כאן משהו שמן?"**
>
> **`run_self_test()` מאמת מול צורות שממדיהן ידועים** — מוט, מוט מסובב, **חוט מעוקל**, דיסקה, וחוט-עם-טיפה — כי אומדן שאיש לא בדק הוא בדיוק איך הגענו לכאן. *(לקח לוואי: שתי הפיקסטורות הראשונות נכשלו כי `cv2.line` בעובי 8 על 45° מרנדר 11.3px — בדקתי את הפיקסטורה, לא את האומדן.)*

**מה לא הועבר, בכוונה:** וטו הקמט (`CREASE_REPAIR_ELONGATION=3.0`) כבר מבוסס-DT ולכן אינו נושא את הבאג, אבל המוסכמה שלו שונה (חלוקה בחצי-העובי בנקודה השמנה) והקבוע כויל מול קיפולים נמדדים ב-321A5173. **לבטא מחדש קבוע מדוד בלי הפיקסטורות למדוד עליהן זה ניחוש** — האקוויוולנט הצפוי הוא ~1.5.

## 10.4 `engine/test_tool_parity.py` — חוזה הכלי מוצהר בארבעה מקומות

`src/toolRegistry.ts` · `engine/server.py` · `engine/render.py` · `engine/presets.py`. שום דבר לא סנכרן ביניהם. נמצאו ותוקנו:

| הפער | מה זה עשה |
|---|---|
| `skin` הצהיר פרמטר אחד מתוך **חמישה** | `scale/evenness/texture/body` נקראים ב-`skin.py` מאז ומתמיד |
| `background-blur.bokeh` לא הוצהר | נקרא ב-`background.py:79` |
| `hair-tones.strength` = 45 ב-UI מול **50** במנוע | "אפס לברירת מחדל" נתן תוצאה שונה לפי הצד שאיפס |

> **אף אחד לא היה סליידר מת** — המנוע קורא הכל. אבל `/tools` **שיקר** לכל לקוח שסמך עליו, וזה בדיוק המנגנון שהפיל את `skin-cleanup`: ה-UI שלח שמות שהמנוע לא הכיר, המנוע נפל לברירת המחדל שלו, **והסליידרים הפסיקו לשלוט בכלום — בלי שגיאה בשום מקום.**
>
> `pixel-color` נרשם כחריג מוצהר: הוא **מותאם** מזוג תמונות ולא מוגדר, ולכן אין לו סליידרים בכוונה.

## 10.5 סקר 26 תמונות אמיתיות (`engine/_survey_folder.py`) — ספסל המבחן שחסר

הרצה על `Downloads/11` (26 תמונות, 50 פנים), עם גיליון `BEFORE | מועמדים | AFTER` לכל פנים.

| | |
|---|---|
| פנים שסורבו כ"קטנות מדי" | **11 מתוך 50 (22%)** |
| מועמדים בסך הכל | 1,070 |
| תמונות ללא שינוי מהותי | 4 |

> **🔴 הממצא המרכזי — טווח ההפעלה, לא האיכות.** `MIN_FACE_PX=180` נכון בנימוקו, אבל **22% מהפנים בעבודה אמיתית נופלות מתחתיו**. גרוע מכך: ב-`321A5078` יש **5 פנים, 3 סורבו ו-2 טופלו — באותה תמונה**. בתמונה שנמסרת ללקוח, חלק מהילדים מרוטשים וחלק לא; **חוסר-עקביות גלוי גרוע מסירוב אחיד**. ב-`321A4954` כל ארבע הפנים (76-160px) סורבו, ויש שם מה לנקות — סימן ליד נחיר, סימן על סנטר.
>
> **⚠️ `321A1791` — הדגל השני.** תינוקת עם **סומק טבעי חזק מאוד**; 201,927 פיקסלים שונו, והאדמומיות הופחתה בעין. `test_blush` **עובר** (dL=0.323) — אבל זה פי 2-6 מכל שאר התמונות בסט. **סימן אזהרה, לא כשל מוכח.** אם המשמר הזה אי פעם ייכשל, זו התמונה שתפיל אותו.
>
> **⚠️ יחס אות-לרעש בזיהוי.** `321A5254` — פנים 427px שנראות לי **נקיות** — ייצרה **45 מועמדים**. ה-AFTER בטוח (הסומק והמרקם שלמים), אבל הכלי עובד הרבה במקומות שאין בהם דבר. זה לא נזק, זו חשיפה.
>
> **תקלות בקטנה:** `321A5208` מדווח `noSkinRegion` ובכל זאת משנה ~4,300px · `321A5097` מדווח `faces: 0` וגם `faceTooSmall: 1` (רישום סותר).

**מה שנבדק ויזואלית:** 5 גיליונות מתוך 26 (5254, 5173, 4954, 1791, 1809). השאר מדווח במספרים בלבד — **וזה מוצהר ולא מוסתר.**

---

## 11. סטאק וטכנולוגיות

- **Shell:** Electron → EXE. (Tauri כחלופה קלה אם המשקל יכביד.)
- **Renderer:** React + TypeScript + Vite. עברית RTL.
- **Engine:** Python. עכשיו: `numpy`, `pillow`. ל-AI כבד: `torch` + `onnxruntime` + משקלי מודל.
  - ⚠️ **משקל:** torch מוסיף כמה ג'יגה להתקנה/אריזה. שוקלים ONNX Runtime כדי לרזות. להחליט לפני האריזה.
- **RAW:** `rawpy`.
- **כלל רישוי:** MIT / Apache / BSD בלבד.

---

## 12. סדר בנייה (כל שלב = פרוסה אמיתית, נבנית כדי לשמור)

- **Phase 0 — בסיס** ✅ (בוצע): renderer + כלים גלובליים + מתכון + "החל על מסומנות" + POC עור.
- **Phase 1 — איחוד ה-Backbone** ✅ (בוצע): `ToolDef` אחיד + `toolRegistry` + `ToolInstance[]`; החלקת-העור היא **כלי במתכון** (לא דורס קנבס); pipeline: local-ai (מנוע, cached) → global (JS, חי); אצווה נושאת את המתכון המלא כולל AI. **ביצועי AI נמדדו: ~210–244ms לתמונה** (הסיכון שסומן — תקין). *(מעבר לנתיבי-קובץ נדחה ל-Phase 5/Electron; בדפדפן עדיין data URLs.)*
- **Phase 1.5 — גל הכלים הגלובליים** ✅ (בוצע): 7 כלים חדשים ברישום (תלת-מימדיות, צבעוניות, נקודת אור, גלואו, ציור שמן, חידוד) + הרחבת טון-וצבע ב-`whites`/`blacks`. הפאנל מרנדר **גנרי מהרישום** (קבוצות מתקפלות) — כלי חדש = שורה ברישום בלבד. מכסה 10/16 מרשימת הצלמת.
- **Phase 1.6 — MaskProvider + כלי AI שני** ✅ (בוצע): `engine/masks.py` (MediaPipe, 6 סוגי מסכות) + **טשטוש רקע**. שלב ה-AI ב-Editor הוכלל לשרשור **כל** כלי ה-AI לפי סדר. אומת על תמונה אמיתית: פילוח ~256ms, טשטוש מלא ~410ms, HTTP ~500ms. מכסה 11/16.
- **Phase 2 — שכבה 3** ⏳ *(הבא):* ספריית סגנונות בשם + אצווה דרך המנוע + ייצוא לדיסק + **מבחן parity** JS↔Python.
- **Phase 3 — כלי AI כאזרחים:** טשטוש רקע → עיניים → פצעונים.
- **Phase 4 — RAW** + חשיפה/WB אדפטיביים.
- **Phase 5 — אריזה:** Electron + הדלקת sidecar + מתקין.
- **Phase 6 — צפונה:** קאלינג, אלבום (מודולי החזון).

---

## 13. החלטות פתוחות / סיכונים

- 🔴 **חוסם — שלב ההחלטה בריטוש העור (§5).** סדר הטיפול: **(1)** מסכת אנטומיה אמיתית מ-478 נקודות הציון — מנקה את מפת הזיהוי כדי שתהיה קריאה; **(2)** צורת המסכה: קודם הזול (סגירה מורפולוגית + הגדלת זרע לנגע שלם), ואם לא מספיק — **SAM** כ"תותח כבד" (קלט: זרע משיא ה-novelty; פלט: אזור קוהרנטי). אותו SAM ישרת גם מברשת בקליק אחד, כך שרכיב אחד מכסה את המסלול האוטומטי ואת הידני.
- ⚠️ **רישוי משקולות — לאמת מול הקובץ, לא מהזיכרון.** `advimman/lama`: ה-LICENSE במאגר Apache 2.0, אך **רישיון המשקולות של `big-lama` לא אומת** — והתבנית "קוד פרמיסיבי, משקולות NC" היא בדיוק מה שפסל את CodeFormer ו-BRIA. לבדוק גם עבור SAM לפני התחייבות.
- ⚠️ **אימות על תמונה אחת אינו אימות.** כל העבודה עד כה נשענת על קובץ בדיקה יחיד (ילדה אחת, שריטה אחת). דרושות 3–4 תמונות אמיתיות מהצלמת עם לכלוכים מסוגים שונים (שוקו, במבה, עקיצה) — הן שוות יותר מכל שיפור אלגוריתמי.
- 🟡 **ביצועי מנוע:** שופרו מ-~32s ל-**~12.6s** לתמונת 20MP במתכון של 6 כלים (300 תמונות ≈ 63 דק'). עדיין שווה לשפר; המשך אפשרי: `tone-color` (~4.6s) ו-`background-blur` (~3.0s).
- 🔴 **`_fluid_trails` שגוי במודל שלו, לא בכיול (§5).** מועמדיו הגדולים על תינוק היו **שתי העיניים והנחיר**, והם נדחו במרווח של 0.10 בפרמטר בודד; על ילדה אותו שער נכשל ושכתב 111 יחידות על העפעף. במקביל הוא **אינו יכול** לראות רוק על השפתיים, כי הוא מחפש חוט שיצא מהפה. צריך להיבנות מחדש סביב הפיזיקה הנכונה, ולא לקבל עוד שער.
- 🔴 **ברק רטוב על שפתיים — אין אופרטור.** נדרש מקטין היילייט **ספקולרי** בתוך מסכת השפתיים. אסור שיחיה ב-`_fluid_trails`.
- ✅ **ציור שמן — תוקן (2026-07-29), §10.2.** ‏Kuwahara אמיתי בשני הצדדים, נוסחת רדיוס משותפת ‎0.3%–3.0%‎ מהצלע, ו-`oil-paint` נמצא עכשיו ב-`test_parity.py` (max 1, ‏0ppm).
- 🔴 **מבחן ה-parity JS↔Python לא נכתב, והוא לא אופציונלי.** §8 הצהירה עליו כמיטיגציה לכפילות המכוונת; בהיעדרו ציור השמן התפצל לשני אלגוריתמים בשקט. אותו סיכון פתוח על **כל** שאר הכלים הגלובליים — הם פשוט לא נמדדו.
- ⚠️ **חוזה הכלי מוצהר בשני מקומות** (`src/toolRegistry.ts` + `engine/server.py`) ונפרד בשקט: השרת המשיך להצהיר `disabled: true` על `skin-cleanup` זמן רב אחרי שהמנוע יכול היה יותר, וזו הסיבה שכלי עובד נראה שבור מהצד הקדמי. תוקן נקודתית; הפתרון האמיתי הוא רישום אחד.
- ⚠️ **המנוע הוא sidecar ואינו נטען מחדש לבד.** מודול חדש (למשל `pigment.py`) לא קיים בתהליך שרץ, והוא ממשיך להריץ קוד ישן **בשקט ובלי שגיאה** — וגרוע מכך, אם שמות הפרמטרים השתנו הוא נופל לברירת המחדל שלו והסליידרים מפסיקים לשלוט לגמרי.
- 🔴 **טווח ההפעלה, לא האיכות — הפער הגדול ביותר שהסקר חשף (§10.5).** 22% מהפנים בעבודה אמיתית מתחת ל-`MIN_FACE_PX`, ובתמונה אחת 3 מ-5 ילדים סורבו בזמן ש-2 טופלו. **חוסר-עקביות בתוך פריים אחד גרוע מסירוב אחיד** — לכל הפחות הכלי צריך להחליט ברמת התמונה, לא ברמת הפנים.
- ⚠️ **`321A1791` הוא מקרה המבחן של `test_blush`.** תינוקת עם סומק חזק, 202k פיקסלים שונו, dL=0.323 מול סבולת 0.6 — פי 2-6 מכל תמונה אחרת. לבדוק אותה אחרי כל שינוי ב-`pigment`.
- ⚠️ ביצועי תצוגה מקדימה (Phase 1 — למדוד מוקדם).
- שם ומיתוג המוצר — TBD.
- משקל תלויות AI (torch) מול גודל האריזה — לשקול ONNX.
- סדר ה-pipeline המדויק בין כלי ה-AI — לחדד תוך כדי.
- מודל עסקי — לא חוסם.
