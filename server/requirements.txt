# TEZA backend. Lower bounds only — the dev machine runs Python 3.14, so pip is
# left free to pick wheels that exist for it. Pin exactly when deploying the VPS.
fastapi>=0.115
uvicorn[standard]>=0.32
SQLAlchemy>=2.0
pydantic>=2.9
pydantic-settings>=2.6
bcrypt>=4.2
python-jose[cryptography]>=3.3
cryptography>=43
authlib>=1.3
httpx>=0.27
python-multipart>=0.0.12
email-validator>=2.2
itsdangerous>=2.1
